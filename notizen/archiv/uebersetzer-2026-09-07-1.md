# Logbook — Translator

Rotated by the runner on 2026-09-06 at 12302 characters (cap 12,000). Predecessor: `notizen/archiv/uebersetzer-2026-09-06-1.md`.
Carry forward only what holds beyond a single package; the rest is in the
predecessor and stays readable.

- 2026-09-07 — spiel.md#Befundabarbeitung — Prüfung der Runde 4 vom 2026-09-01 (~3,650 → ~3,620 chars, estimated from lines; no shell). Heading kept German per a72a1b2. Terms chosen: Kappung = capping, Deckelung = ceiling, Lückengrenze = gap bound, Ergebnisband = result band, Entwurfstabelle = draft table, Eintragsart = entry kind, Ergebnisraum = outcome space; reused: slot, check (Probe), test stand, process control, provenance entry, stake, way T44 wording. Cross-ref *Was das Fondsvermögen ist* → live English *What the fund's assets are, and whether the stakes belong to them* (line 512); „Was der Architekt neu rechnen muss" (heading still German, line 2954) and reviewer's „Geprüft und nicht gezählt" kept German. Quotes kept German („über 15 Runden … `3·ai` Steckplätze", „alle übrigen, abschließend"). Numbers localised 1.000/27.000/28.000/27.001/28.001 → comma format per live-line-689 convention; `R ≤ 26`, `5k`/`3k·ai`/`15k` byte-for-byte.
- 2026-09-07 — spiel.md#Befundabarbeitung — Prüfung der Runde 6 vom 2026-09-01 (~4,500 → ~4,550 chars, estimated from lines; no shell). Heading kept German per a72a1b2. Terms chosen: Entstehungsregel = rule for how they arise, Doppelführung = double keeping, Ablaufsteuerung = process control, Regler = knob, Rücklaufzähler = return counter, Beteiligungsvermögen = stake wealth, Klammer (as a patch) = parenthesis; reused: slot, step, way of dying, vintage build, test stand, round boundary, counterforce, price shock, policy rate, wedge-free channel table wording. Cross-ref *Was ein Korb wert ist…* → live English *What a basket is worth, what a step is and what a bond costs* (line 538); *Offene Entwurfsfragen* and „Geprüft und nicht gezählt" (reviewer's German finding) left German. Quotes kept German („für die vier Anleihe-Steckplätze…", „Wie groß ein Anteil…", „sieben", „achten Kanal als Befund", „eine Bewertung, nicht zwei"). Numbers: prose 10.000/100.000 → 10,000/100,000 (matches live line 689); backticked `e ≤ 2.000` byte-for-byte.
- 2026-09-07 — spiel.md#Befundabarbeitung — Prüfung 0021 vom 2026-09-02, Paket 0039 (~2,750 → ~2,780 chars, estimated from lines; no shell). Heading kept German per a72a1b2. Terms chosen: Zerlegung = decomposition, Neubasierung = rebasing, Basiswechsel = base change, Fassung (of a formula) = formulation, Lesetabelle = read table, Zeilenzuordnung = row mapping, Schadensvorschrift = damage rule; reused: work-off, reason for a return, base term, wedge, tariff level, check/play vintage, catch-up table, path 1/2/3, check (Probe). Kept German: cross-refs *Warum die Zollzeile nur den Keil misst* (line 1720), row label *Die Ordnung, die das Argument trägt*, table *Neu aus Paket 0039* (heading at 3112 still German), backticked `Schwere: mittel`/`gering`. Formula `112 + 8 − 16 + 2 =` **106** and all counts byte-for-byte.
- 2026-09-07 — spiel.md#Befundabarbeitung — Prüfung 0039 vom 2026-09-03, Paket 0055 (~2,540 → ~2,560 chars, estimated from lines; no shell). Heading kept German per a72a1b2. Terms chosen: Regressionsfall = regression case, Abarbeitung (of a finding) = work-off, Rücklaufgrund = reason for a return; reused: Sockel = base term, Ursachenkette = causal chain, Preisträgheit = price inertia, Prüfer = reviewer, Weg/Zustand A/B = path/state A/B (match lines 1533/1891). Numbers 32.850.000, 8.472.000, `10.795,2`, 10.000 kept byte-for-byte (live English passages at 1776/1898–1908 use German format); decimal commas 1,5·10^9 / 1,4·10^8 localised to dots per earlier runs. Backticked `Schwere: gering`, `geprueft` unchanged. Cross-ref *Warum die Zollzeile nur den Keil misst* left German (heading still German at line 1720).
- 2026-09-07 — spiel.md#Befundabarbeitung — Prüfung der zweiten Fassung vom 2026-08-31 (~4,700 → ~4,750 chars, estimated from lines; no shell). Heading kept German per a72a1b2. Terms chosen: nicht belegt = not substantiated, Fehlweg = wrong path, Grobzuordnung = coarse mapping, Artenzusammensetzung = composition of kinds, Gleichstandsregel = tie rule, Abnahmehälfte = acceptance half, „plus 24 Trägerpartie" (elliptical) = "plus 24 for the carrier game"; reused: check vintage, stock procedure, way of dying, excess function, bisection search (T28, line 307), strategy core, vintage spread, capping, trade block, search bot. Self-quotes to live English: death rule → „fund assets ≤ 0 triggers way of dying 1 in the same round" (rule live English at 534–536), band „mandate fulfilled" (line 2250); table cross-ref → *Every quantity without a data anchor, with its start value* (line 465). Numbers: prose 10.000/1.500/9.000/0,4 localised; backticked `10.000·H/(H+N)`, `{0; 0,4; …; 2,0}`, `R × 1.000 = 24.000`, `E(p) ≤ 24.000` byte-for-byte.
- 2026-09-07 — spiel.md#Was der Architekt neu rechnen muss incl. all 8 subsections down to *Aus Fassung 3* (~19.4k → ~19.2k chars, estimated from lines; no shell). Headings kept German per a72a1b2. Terms chosen: Adressaufstellung = address roster, Deckungsrechnung = coverage calculation, Stellschraube = tuning knob, Gewinnschwelle = profit threshold, Ratenreihen = rate series, Zollstand = tariff level, Aufschlag = surcharge, Herkunftseintrag = provenance entry, Abzählschritt geht auf = count-off step comes out even, volkswirtschaftlich = macroeconomic, Aufnahme (in technik.md) = incorporation, geltend = governing; table headers war/ist/betrifft = was/is/affects (and „ist"-column self-ref → „is"). Kept German: replacement wording destined for technik.md („bei R = 20", „Die 27 Sollreihen sind…", „20-Runden-Partie", „acht"/„einen neunten"), quoted technik.md/old wording, Festlegung/Folgerung/Stufe labels, band „überlebt". Cross-refs to live English headings updated (*The tariff wedge is multiplicative*, *What a basket is worth*, *Every quantity without a data anchor*, step „settlement"); still-German headings left German (*Welche neun Länder*, *Die Folge aus Weg A*, *Was ein Börsenplatz trägt*, *Der Schaden in Gegenkraft 5*, *Offene Entwurfsfragen*, *Neu aus Paket 0054*). Numbers: prose localised (2,310; 9.54 billion; 26.5; 222.7; 0.08), backticked formulas byte-for-byte (dots kept).
- 2026-09-07 — spiel.md#Offene Entwurfsfragen (~11.5k → ~11.3k chars, estimated from lines; first 3 bullets were already English, 22 translated). Heading kept German per convention. Terms chosen: Lastsatz = load rate, Bankzins = bank lending rate, Größenordnungsprobe = order-of-magnitude check, Suchbot = search bot, Handelsblock = trade block, Rückvergleichsland = backtest country, Platz (Börsenplatz) = venue, Regierungswechsel = change of government. Kept German: manifest values „Rückvergleichsland"/„Spielland" (quoted), instrument name „Finanzmarktregulierung", T63 tier „Stufe 1", citation „T62 Folgerung 2". Self-quotes updated to live English wording: „action 3, netting in step 3" (state table line ~486), „pure model construct"/without a target series (instrument table ~463). Numbers: decimal commas localised (76.6, 51.3, 47.7, 2.1 · 10^10, 1.05 · 10^8); thousands dots 10.000/10.500 kept to match live counterforce-5 text („per 10.000 thousand USD") and code; formula `max(Ek) ≤ 1,25 · min(Ek)` kept byte-for-byte per live Maß wording.
- 2026-09-07 — spiel.md#Was bewusst fehlt (flat bullet list, no subsections; ~14.4k → ~14.2k chars, estimated from lines). Heading kept German per convention. Terms chosen: Spiel-/Start-/Prüfjahrgang = play/start/check vintage, Sollmaske = target mask, Instrumentensperre = instrument lock, Kursbindung = currency peg, Handels-/Zoll-/Schadenszeile = trade/tariff/damage row, Vorleistungsmatrix = intermediate-inputs matrix, Inhaltslaufband = content treadmill, Kaufangebot = sales offer, Zustimmung = approval, Takt = time step, Kettenansicht = chain view, Kreislauf = circular flow. Number formats localised (3,75 → 3.75; 87.788 → 87,788 etc.), values unchanged. Instrument name „Finanzmarktregulierung" and `Maß` kept. Cross-refs: *Die Partielänge R* → *The game length R* (live English heading); *Was die Schicht kostet*, *Warum die Zollzeile nur den Keil misst* left German (headings still German).
- 2026-09-07 — spiel.md#Wie die vier Masse berechnet werden incl. all 9 subsections (~22,7k → ~22,5k chars, estimated from lines; no shell). Headings kept German per a72a1b2; 3 code blocks (formulas/cost table) kept byte-for-byte incl. German words per rule 3. Terms chosen: Fehlbetrag = shortfall, Vorrat(sverfahren) = stock (procedure), Aktionsart = action kind, Kennung = id, Startwert (test stand) = seed / (state field) = start value, Trägerpartie = carrier game, Bündel = bundle, Jahrgangsstreuung = vintage spread, Richtungstreue = directional accuracy, reißen (Prüfgegenstand) = break, Sammelkappe = catch-all cap, Verschiebung = shift, Strategiekern = strategy core, Sollstelle = target point, Prüfjahrgang = check vintage, Nachtlauf = night run. Seed-derivation tuple (Jahrgang, Parametersatz, Wurzelstartwert, t, Stichprobenindex) kept German — likely config identifiers. Self-quotes translated („survived", „mandate fulfilled", „at most 2 of the 16"); review quotes already English; instrument name „Finanzmarktregulierung" kept. Cross-refs: *Die Partielänge R* → *The game length R* (live English heading); *Welche neun Länder*, **Der Fehlbetrag**, „Keine verdeckte Größe" left German (headings still German).
- 2026-09-07 — spiel.md#Die Gegenkraefte incl. all 12 subsections (~40,4k → ~40,1k chars, estimated from line count; no shell to measure). Headings kept German per a72a1b2. Terms chosen: Sockel = base term, Nachahmer = imitator, Jahrgang = vintage (Jahrgangsbau = vintage build, Jahrgangskonstante = vintage constant), Keil = wedge, Markträumung = market clearing, Rundengrenze = round boundary, Prüfstand = test stand, Selbstspiel = self-play, Nachziehtabelle = catch-up table, Gegendruck = counter-pressure (address `gegendruck` stays), Zerfall = decay, Einschwingen = transient. Self-quotes now English to match my own translation of the quoted passages („It is business cycle, not reaction …", „The price it sets", „counter-pressure acts only in the following round"); quotes citing the already-English #What must hold for price formation use its live wording verbatim; quotes citing German docs (T28/T38/T5/T47, „proportional zu", „Finanzmarktregulierung") kept German. Cross-refs updated to English where target heading is English (*What must hold for price formation*, *What a basket is worth*, *Does it grow with success?*); refs to still-German headings left German.
- 2026-09-07 — spiel.md#Die drei Schichten der Welt incl. 7 subsections (~19,700 → ~19,400 chars); headings kept German per a72a1b2 (they are cite keys). Terms chosen: Schicht = layer, Andrang = crowding, Preisstoß = price shock, Marktenge = market thinness, Politische Gegenreaktion = political backlash, Bruchtester = break tester, Pflichtenheft = specification, Zuschnitt = cut, Lizenzstelle = licence point, Beharrungswert = steady state, Weltschritt = world step, Schranke = bound; Aufsicht = supervision (matches supervision counter from #Die Aktionen run). Cross-refs *The actions* updated to the live English heading; *Welche neun Länder*, *Die Grenze des Orakels*, *Was bewusst fehlt* left German (still German in file). Quotes kept: „Zwanzig Indizes…", „Eine Zinswette…", „Wie fein der Aktionsraum sein darf", the three English review quotes.
- 2026-09-06 — spiel.md#Die Aktionen → #The actions: ~2,050 → ~2,030 chars. Terms chosen: Beteiligung=stake, Stufe=(fixed) step, Leitzins=policy rate, Aufsichtszähler=supervision counter, Todesart=way of dying, Gegenlobby=counter-lobby; `Maß` kept as identifier (matches earlier sections). Cross-refs to still-German headings left German.
- 2026-09-06 — spiel.md#The loop: empty run, FIFTY-SIXTH dispatch; re-verified lines 262–384 fully English, no edit. **Operator: this queue entry is stale — 56 dispatches, 55 wasted runs on a section translated 2026-09-06 as #Die Schleife (see archive). The dispatcher re-queues it under its translated heading; only closing the entry at the source stops it — no translator run can.** The 35 identical per-dispatch lines (dispatches 21–55, all same date, all empty, no edit ever needed) are consolidated into this one; the previous logbook state is in git history.
