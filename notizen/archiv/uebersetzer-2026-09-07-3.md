# Logbook — Translator

Rotated by the runner on 2026-09-07 at 13073 characters (cap 12,000). Predecessor: `notizen/archiv/uebersetzer-2026-09-07-2.md`.
Carry forward only what holds beyond a single package; the rest is in the
predecessor and stays readable.

- 2026-09-07 — technik.md#10. Was das Modell an Rechenzeit kostet (lines 2626–2745, ~5.9k → ~5.8k chars, estimated from lines; no shell). Heading kept German per a72a1b2. Terms reused from live English (spiel.md:3390 table, T50): plan value, night run, break run, boundedness, backtest, regression stock, decision density, strategy diversity, shift of the optimum, seed = Startwert, carrier game, test bench, territory, "exactly one call site" (T50 quote now English). New choices: Vorrunde = previous round (lies_alt context), Zugriffsvorgänge = access operations, Prüfbefund = check finding, Zollstand/Zollzeile = tariff state/row, Kalibrierschleife = calibration loop, Gegenrechnung = counter-calculation, Kettenverlauf = chain history, Käufer = buyer, Mitschreiben der Kette = recording of the chain. Kept German: spiel.md section name *Wo die Regel läuft, und warum Kanal 3 zyklenfrei bleibt*; backticked byte-for-byte: `30 × 300 = 9.000`, `1.464 = 24 × 61`, `3.689.280 = 126 × 20 × 1.464`, `11.519.040 + 240.000 + 200 + 24 + 24.000 = 11.783.264`, `112 + 8 − 16 + 2 = 106`, `Gebiet × Gegenüber × handelbarer Sektor = 5 × 4 × 2`. R = 24 numbers translated as written per rule 5 (R = 20 substitution lives in spiel.md:3387, not here). Ledger line for #10 (and still #9) needs the runner/operator — ops/ write denied for this role.
- 2026-09-07 — technik.md#18. Paket `0043-t48-groessen-gegenkraft-5` — Umfang und die Meldung an den Projektmanager (lines 3191–3272, ~6.1k → ~6.1k chars, per index; no shell). Heading kept German per a72a1b2. Terms reused from live English T48/section 10 (technik.md:2195–2232, 2643ff): collection (Erhebung), remainder/remainder table (Rest/Restetabelle), arity (Stelligkeit), case distinction, market clearing, counterforce 5, read/write accesses, calls, prescription, acceptance, territory, reviewer. New choices: Auftrag = brief (avoids collision with Reihenfolge = order/sequence), Kostenzeile = cost line, Lesetabelle = read table, einstellig/zweistellig = one-argument/two-argument, Formänderung = change of form, Datenbauer = data builder, Kernbauer = core builder, Nachweis = proof, Definitionsbereich = domain. Kept German quotes: „siebzehn", „Unten stehen siebzehn", „je Gebiet"; header comment `//! kern::werte -- die siebzehn abgeleiteten Groessen aus T48` byte-for-byte. Possible source error translated as written (rule 5): "die zwölf erlaubten Reste" says twelve, but the live T48 remainder table (technik.md:2205ff) says twenty-two names in ten lines.
- 2026-09-07 — technik.md#17. Paket `0026-klasse-2-preisbasis` — Umfang, und was ausdrücklich liegen bleibt (lines 3064–3190, ~9.2k → ~9.2k chars, per index; no shell). Heading kept German per a72a1b2. Terms reused from live English: return (Rücklauf), trade (Gewerk), check, finding, work-off, prescription, support points, ten-thousandths, check vintage → vintage, target series, scoping/collision case (technik.md:142), messages (technik.md:3051), brought up to date; series-list row 20 table header copied from live section 7 (technik.md:1776: Nr | Quantity | Dimension | Model unit | Source | Role | Suspect), column reference „Rolle" → "Role" to match. New choices: Kopfwert = headline value, Kopfraum = headroom, Entblockungssatz = unblocking sentence, Übertrag = transfer, Umrechnungsgröße = conversion quantity, Restgrösse = residual quantity, Werkzeugkette = toolchain, Vorspann = preamble, nachgesehen statt vermutet = looked up rather than presumed. Kept German quotes: „Was der Geschäftsführer entscheiden lassen muss", „Setze `status: gebaut`", „vier Stellen und sonst keine", „T1 bis T52"/„T1 bis T53" (historical preamble wording, no longer live), search strings „Reihe 20"/„Ausfuhrpreisindex"/„Deflator", unblocking-sentence quote, `abgeleitet aus Reihe 14 (H) und Reihe 2 (N)`. Backticked byte-for-byte: `r(2021) = 11.549/7.417 = 1,55710`/`= 1,557098`, `10.000/7.417 = 1,348254`, `10.000 · |7.417 − Index(t)| / Index(t)`, `15 + 15 + 40 + 1 = 71`; prose numbers localised (3,577.80/3,578; 1.5571/1.5570; 2,203/2,202.98; 2,000; 55,074.54; 10,000/7,417 = 1.34825; 7,288 → 7,837; 5,464 → 6,189; 7,836.99; 6,189.15; 2.8 · 10^17/9.2 · 10^18; factor 1 against 1,000), matching T53 live English (technik.md:513f).
- 2026-09-07 — technik.md#16. Befundabarbeitung — Prüfung zu Paket `0011-stack-auf-cpp`, Runde 1 vom 2026-09-03 (lines 3004–3063, ~4.4k → ~4.4k chars, per index; no shell). Heading kept German per a72a1b2. Terms reused from live English: fixed (= behoben, technik.md:2991), measure (= Massnahme, :731ff), guard (= Wächter, :771), promise (= Zusage, :228), narrowing (= Verengung, :766), abort path (:804), addition and subtraction (= Strichrechnung, :791), third-party library, prescription, return, version, reviewer, venture, brought up to date; Maß kept as name (the Maße) per :898ff. New choices: Nachbesserung = rework, Bauart = way of building, Rechenart = kind of arithmetic, Mustervergleich = pattern match, Verabredung = agreement, Probe = test / Probenziel = test target, Zuordnung = mapping, Zwillingsform = twin form, Größenmultiplikation = quantity multiplication. Kept German: „anders gelöst"; backticked/identifiers byte-for-byte: `mal(a, b)`, `<ziel>`, both grep commands, `fremd/<name>/`, T-numbers, 310, 52, six, packages 0004/0031.
- 2026-09-07 — technik.md#15. Befundabarbeitung — Prüfung der Runde 6 vom 2026-09-01 und `spiel.md` Fassung 5 (lines 2901–3003, ~8.3k → ~8.3k chars, per index; no shell). Heading kept German per a72a1b2. Terms reused from live English: working-off (#14), check, version, reviewer, prescription, test bench, world step, night run, call site (T50), way of dying, vintage constant, support point, target mask, brought up to date, policy rate. New choices: Gewerk = trade, Entstehungsregel = formation rule, Skalengrenze = scale boundary, Skalenklasse = scale class, Skalenmischung = scale mixture, volkswirtschaftlich (korbwert) = macroeconomic, Zahlenprobe = numerical check, Zulässigkeitsfrage = admissibility question, Kalibrierbedingung = calibration condition, Herkunftszerlegung = origin decomposition, Rücklaufzähler = return counter, Beteiligung = stake, Kasse = cash, Fondsvermögen = fund assets, Maß = measure, Ergebnisbänder = result bands. Kept German: quotes „T45s Abzählschritt …", T48 rule „ein Name, der weder Adresse …", „Modellmarktwerten", „sieben Kanäle" → acht row, „eine Bewertung, nicht zwei", „rund zwanzig Operationen, drei Promille", „Geprüft und nicht gezählt"; spiel.md section names *Was der Architekt neu rechnen muss*, *Wo die Skalengrenze liegt*, *Offene Entwurfsfragen*. Backticked byte-for-byte: `21.000.000.000 · 20 / 10.000 = 42.000.000`, `136 + 150 + 11 + 2 + 11`, both 310/44 decompositions, `aufschlag_min = 1 − min(leitzins_pfad)`; prose numbers localised (100,000; 4,200,000,000,000 cents; 42 billion/21 trillion/42 million USD; 0.2 %; 11,519,040; 11,783,264). R = 24 kept per rule 5.
- 2026-09-07 — technik.md#14. Befundabarbeitung — die vier früheren Prüfungen (lines 2883–2901, ~1.2k → ~1.2k chars, per index; no shell). Heading kept German per a72a1b2. Terms reused from live English: check (= Prüfung), version (= Fassung), prescription (= Vorgabe), reviewer. New choice: Abarbeitung = working-off (distinct from Bearbeitung = processing, technik.md:2758). Quote kept German: „Beide Befunde der Vorrunde … nicht gegen die Behauptung." (check of round 6). Path `git log -p specs/0016-…/technik.md` byte-for-byte; T38–T47 numbers untouched.
- 2026-09-07 — technik.md#13. Hinweis für den Projektmanager (lines 2858–2882, ~1.6k → ~1.6k chars, per index; no shell). Heading kept German per a72a1b2. Terms reused from live English: component (T13 title, technik.md:1036; note line 142 says "Module direction" — both live), collision cut (technik.md:142), vintage build, policy rate, venture, support points, trade flows, backtest, version (= Fassung). New choices: vorzuziehen = to be pulled forward, Entwurfsrisiko = design risk, Bauaufwand = build effort, Verdachtsreihen = suspect series, Drittanbieter-Ausnahme = third-party exception, Indiz = indication, kippen (Vorhaben) = tip the venture. Backticked byte-for-byte: `aufschlag ≥ aufschlag_min`; module names `kern`/`daten`/`schnittstelle`/`konsole`/`pruefstand`/`oberflaeche`/`werkzeuge/aufbereitung` untouched.
- 2026-09-07 — technik.md#12. Was ich nicht entschieden habe (lines 2764–2857, ~7.5k → ~7.5k chars, per index; no shell). Heading kept German per a72a1b2. Terms reused from live English: world step, night run, self-player, heuristic bot, search bot, halving step, vintage build, target mask, instrument tier (Klasse 10) vs instrument level (Instrumentenstand, technik.md:2113), counterforce 5, lobby point, numéraire, support points, backtest, capping, way-of-dying context untouched. New choices: Rückgabe = hand-back, gleichstandsfrei = tie-free, Spielraum = latitude, nachgeführt = brought up to date. Kept German quotes: „laut statt still", „ein Schritt", „zwei Herren über eine Zahl", „eine Position öffentlich offenlegen", band „überlebt" (matches spiel.md:3423/3519; note technik.md:910 says band „survived" — both live), section name *Der Schaden in Gegenkraft 5, als Rechenvorschrift*, struck-through „Schaden". Backticked byte-for-byte: `24 × (1 + 60 × 61) = 87.864`; prose numbers localised (1,464; 664 million; 1.8/1.2 hours; 30,000). R = 24 kept as written per rule 5.
- 2026-09-07 — technik.md#11. Auslieferung, und wo Geld anfängt (lines 2746–2763, ~1.1k → ~1.1k chars, estimated from lines; no shell). Heading kept German per a72a1b2. Terms: Zielkennung = target id (Kennung = id per #9), Beglaubigung durch Apple = notarization by Apple, Vorgabeweg = default path, Auslieferungsauflage = delivery requirement, Jahrgang = vintage (matches Prüfjahrgang, #9), Datenaktualisierungslauf = data-update run, Prüfung 3 = check 3. Kept German: menu page name „Datenquellen", product sentence „Die Ausgangsdaten wurden aggregiert … verantworten diese Bearbeitung nicht" (prescribed wording from daten.md).
- 2026-09-07 — technik.md#9. Test- und Prüfstandsaufbau (T30, T31, T34, T35, T43, T41, T44, T36, T37, T42; lines 2234–2625, ~18.9k → ~18.6k chars, estimated from lines; no shell). Heading kept German per a72a1b2; `B(z)` code block byte-for-byte incl. German annotations. Terms chosen (live wording reused where it exists): Bruchlauf = break run (spiel.md:3400), Bruchtester = break tester, Rückvergleicher = backtester, Testentwickler = test developer, Wirkungskette = effect chain, Vorratsinvariante = stock invariant (stock procedure, spiel.md:2554), reissen = breach, Beschränktheit = boundedness, Abweichungsbericht = deviation report, Ziehregel = draw rule, Verwerfungsschleife = rejection loop, Anfangsstück = prefix, Einspeisung = injection (spiel.md:2606), Trägerpartie = carrier game, Aktionsart = action kind / Art = kind, Kennung = id ("on a tie, the smaller id", spiel.md:2562), Auffindereihenfolge = order of discovery, Ergebnisgröße = result quantity (spiel.md:542), Strategiekern = strategy core, Fensterlogik = window logic, Nachspiel = playout, Prüfjahrgang = check vintage, Niveaureihen/Ratenreihen = level/rate series, Richtungstreue = directional accuracy (spiel.md:172), verwertbar = usable, Berichtspflicht = reporting duty; Maß 1/2/3, Todesart = way of dying and death band per live spiel.md. Quotes kept German: „über 15 Runden … `3·ai`" (spiel.md versions 2/3), „Nachspiel mit dem Heuristikbot als Fortsetzung" (version 3 self-quote), „rund zwanzig Ganzzahloperationen, also drei Promille" (version 5 self-quote), „feste Folgepolitik" (spiel.md, no live English equivalent found), *geprüft und nicht gezählt* (reviewer's finding); band „mandate fulfilled" in live English. Numbers: prose/tables localised (10,000; 0.4; 0.6; 2,000; 6,000; 28,001 … 30,999; 1,464); backticked byte-for-byte (`9,2 · 10^13`, `1.000 … R × 1.000 + 30.000`, `v ≤ 1.000`, `25.000 ≤ B ≤ 28.000`, `60 · 300 + 24 = 18.024`). R = 24 / 25 support points / 31 series / 16 subjects translated as written per rule 5 (known drift to R = 20 / 21 / 27 / 28, tracked since section 2). Ledger line `specs/0016-hedgefonds-simulation-echte-weltwirtschaft/technik.md#9. Test- und Prüfstandsaufbau` needs the runner/operator (ops/ write denied for this role), or the dispatcher re-picks this section.
