# Logbook — Translator

Rotated by the runner on 2026-09-07 at 12459 characters (cap 12,000). Predecessor: `notizen/archiv/uebersetzer-2026-09-07-1.md`.
Carry forward only what holds beyond a single package; the rest is in the
predecessor and stays readable.

- 2026-09-07 — technik.md#2. Der deterministische Kern (T4–T12, T40, T49/T50/T52/T53; ~50.6k → ~50.1k chars, estimated from lines; no shell). Heading kept German per a72a1b2. Terms chosen: Größenklasse = size class, Instrumentenstufe = instrument tier (matches T63 „tier"), Positionsstufe = position step, Kennungen (class 12) = ids, Erwerbstätige = labour force, Anlegerbestand = investor stock, Skalenübergang = scale transition, Aufrufort = call site, ausgewiesene Nichtentscheidung = declared non-decision, Verengung = narrowing, Wächter = guard, Strichrechnung = addition and subtraction, streuende Behälter = hashing containers, Speicher-/Spielstand = save, Vorverarbeiter = preprocessor, Wurzelstartwert = root seed, Stützstelle = support point; Prüfstand = **test bench** in technik.md (matches its live section 1; spiel.md uses „test stand" — per-file consistency wins). Quotes: German kept for former-version self-quotes (T50 struck sentence, „Additionen und Subtraktionen", ADR 0011 „im Nachtlauf laut statt still", „constant 2015 US$", „in thousands current USD"); live-English spiel.md wording used for damage rule, durchgriff ordering, „`handelsvolumen` stands per T5 in class 2 at constant prices", „at most one step per round", „fraction", intra-year swing, bands „mandate fulfilled"/„survived", section „The loop"; T5 self-quote „in this table and nowhere else" now English (quoted line translated in same run). Numbers: prose/tables localised (incl. WDI index table, 2,203/2,000 MAPE, T40 band cells); backticked byte-for-byte (`10.000 / 7.417 = 1,34825`, `8,5·10^37`, `r = 1,5571`); kept 10.000 in the gegenlobby/rabatt parameter sentences to match live counterforce-5 „per 10.000 thousand USD". Cross-refs to still-German headings left German (*Der Schaden in Gegenkraft 5, als Rechenvorschrift*). Noted, not fixed (rule 5): section still says R = 24 / window 1997–2021 / 25 support points; spiel.md has since moved to R = 20 / 2001–2021 / 21 — tracked in *Was der Architekt neu rechnen muss*, not a translation matter. **ops/uebersetzt.txt write DENIED (permission settings) — the ledger line `technik.md#2. Der deterministische Kern` must be added by the runner/operator, or the dispatcher will re-pick this finished section (see the 56-dispatch lesson).** *(Resolved: line present in the ledger on 2026-09-07.)*
- 2026-09-07 — technik.md#3. Trennung von Modell und Darstellung (T13–T14; ~4.4k → ~4.3k chars, estimated from lines; no shell). Heading kept German per a72a1b2; dependency diagram code block kept byte-for-byte incl. its German annotations (rule 3). Terms chosen: Baustein = **component** (Modul = module, reserved for `kern::werte`; affects Bausteinschnitt in section 13 when it comes up), Kopfverzeichnis = header directory, Prüfregel = check rule / Mustervergleich = pattern match / Werkzeugeigenschaft = tool property (all matching live section 1), Nullabhängigkeit = zero dependency, Vorfassung = former version, abgeleitete Größen = derived quantities, Zustandsebenen = state levels, Textoberfläche = text interface. Identifiers untouched: `kern::werte`, `kern/include/kern/werte.hpp`, `tsd_in_cent`, `lobbypunkte_aus_geld`, `lobbypunkte_aus_schaden`, grep patterns, `<ziel>` placeholder in `target_link_libraries(<ziel> …)`. Ledger line `specs/0016-hedgefonds-simulation-echte-weltwirtschaft/technik.md#3. Trennung von Modell und Darstellung` again needs the runner/operator (ops/ write denied for this role; worked via this note last time).
- 2026-09-07 — technik.md#4. Datenmodell (T15–T19, T17b, T38, T39; ~19.2k → ~19.0k chars, estimated from lines; no shell). Heading kept German per a72a1b2. Terms chosen: Sollmaske = **target mask**, Regressionsbestand = **regression corpus**, Sammelschreibweise = collective notation, Verzeichnis (for `daten/adressen.md`) = register, Fundstellen = occurrences, Speicheranforderung = memory allocation, Stufenzahl = step count, Todesart = manner of death, Jahrgangskennung = vintage id, Mandatsstand = mandate status, Zyklenfreiheit = cycle-freedom; identifiers untouched (`Ursachensatz`, `Schreiber`, `Verlauf`, `Vortrag`, `gebietspraefix()`, all T18 cause variants). Citations switched to live-English spiel.md wording: "The state", "The rest of world", "Trade", "The fund", "What must hold for price formation", *What a basket is worth*, index 10,000 (spiel.md: "vintage's start value (index 10,000)"). Kept German: T28 quote „mit `landespreis` = … nach T39", T45 row `Vorgabe(T-Nummer)` + „`restwelt.basiswechsel` → 0 (T8)", channel-8 chain *Vermögen → Lobbybudget → Instrument → Kurs → Bewertung → Vermögen* (verbatim spiel.md:777, still German), „Bewertung", „statt …", *Was hier offen bleibt und hier nicht entschieden wird* (daten/adressen.md). T39-row self-quote now English ("With that `landespreis` has its place") — quoted paragraph translated in same run, per the T5 precedent. Grep-count invariant checked: every `gebiet.`/`gebiet\.` occurrence carried over one-for-one, so the 8/2/17 table still holds for the translated document including itself. Numbers localised in prose (2,480 bytes, 2.5 kB, 10,000); backticked arithmetic byte-for-byte. Ledger line `specs/0016-hedgefonds-simulation-echte-weltwirtschaft/technik.md#4. Datenmodell` again needs the runner/operator (ops/ write denied for this role).
