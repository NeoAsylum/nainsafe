# Logbook — Translator

Rotated by the runner on 2026-09-06 at 12302 characters (cap 12,000). Predecessor: `notizen/archiv/uebersetzer-2026-09-06-1.md`.
Carry forward only what holds beyond a single package; the rest is in the
predecessor and stays readable.

- 2026-09-07 — spiel.md#Was bewusst fehlt (flat bullet list, no subsections; ~14.4k → ~14.2k chars, estimated from lines). Heading kept German per convention. Terms chosen: Spiel-/Start-/Prüfjahrgang = play/start/check vintage, Sollmaske = target mask, Instrumentensperre = instrument lock, Kursbindung = currency peg, Handels-/Zoll-/Schadenszeile = trade/tariff/damage row, Vorleistungsmatrix = intermediate-inputs matrix, Inhaltslaufband = content treadmill, Kaufangebot = sales offer, Zustimmung = approval, Takt = time step, Kettenansicht = chain view, Kreislauf = circular flow. Number formats localised (3,75 → 3.75; 87.788 → 87,788 etc.), values unchanged. Instrument name „Finanzmarktregulierung" and `Maß` kept. Cross-refs: *Die Partielänge R* → *The game length R* (live English heading); *Was die Schicht kostet*, *Warum die Zollzeile nur den Keil misst* left German (headings still German).
- 2026-09-07 — spiel.md#Wie die vier Masse berechnet werden incl. all 9 subsections (~22,7k → ~22,5k chars, estimated from lines; no shell). Headings kept German per a72a1b2; 3 code blocks (formulas/cost table) kept byte-for-byte incl. German words per rule 3. Terms chosen: Fehlbetrag = shortfall, Vorrat(sverfahren) = stock (procedure), Aktionsart = action kind, Kennung = id, Startwert (test stand) = seed / (state field) = start value, Trägerpartie = carrier game, Bündel = bundle, Jahrgangsstreuung = vintage spread, Richtungstreue = directional accuracy, reißen (Prüfgegenstand) = break, Sammelkappe = catch-all cap, Verschiebung = shift, Strategiekern = strategy core, Sollstelle = target point, Prüfjahrgang = check vintage, Nachtlauf = night run. Seed-derivation tuple (Jahrgang, Parametersatz, Wurzelstartwert, t, Stichprobenindex) kept German — likely config identifiers. Self-quotes translated („survived", „mandate fulfilled", „at most 2 of the 16"); review quotes already English; instrument name „Finanzmarktregulierung" kept. Cross-refs: *Die Partielänge R* → *The game length R* (live English heading); *Welche neun Länder*, **Der Fehlbetrag**, „Keine verdeckte Größe" left German (headings still German).
- 2026-09-07 — spiel.md#Die Gegenkraefte incl. all 12 subsections (~40,4k → ~40,1k chars, estimated from line count; no shell to measure). Headings kept German per a72a1b2. Terms chosen: Sockel = base term, Nachahmer = imitator, Jahrgang = vintage (Jahrgangsbau = vintage build, Jahrgangskonstante = vintage constant), Keil = wedge, Markträumung = market clearing, Rundengrenze = round boundary, Prüfstand = test stand, Selbstspiel = self-play, Nachziehtabelle = catch-up table, Gegendruck = counter-pressure (address `gegendruck` stays), Zerfall = decay, Einschwingen = transient. Self-quotes now English to match my own translation of the quoted passages („It is business cycle, not reaction …", „The price it sets", „counter-pressure acts only in the following round"); quotes citing the already-English #What must hold for price formation use its live wording verbatim; quotes citing German docs (T28/T38/T5/T47, „proportional zu", „Finanzmarktregulierung") kept German. Cross-refs updated to English where target heading is English (*What must hold for price formation*, *What a basket is worth*, *Does it grow with success?*); refs to still-German headings left German.
- 2026-09-07 — spiel.md#Die drei Schichten der Welt incl. 7 subsections (~19,700 → ~19,400 chars); headings kept German per a72a1b2 (they are cite keys). Terms chosen: Schicht = layer, Andrang = crowding, Preisstoß = price shock, Marktenge = market thinness, Politische Gegenreaktion = political backlash, Bruchtester = break tester, Pflichtenheft = specification, Zuschnitt = cut, Lizenzstelle = licence point, Beharrungswert = steady state, Weltschritt = world step, Schranke = bound; Aufsicht = supervision (matches supervision counter from #Die Aktionen run). Cross-refs *The actions* updated to the live English heading; *Welche neun Länder*, *Die Grenze des Orakels*, *Was bewusst fehlt* left German (still German in file). Quotes kept: „Zwanzig Indizes…", „Eine Zinswette…", „Wie fein der Aktionsraum sein darf", the three English review quotes.
- 2026-09-06 — spiel.md#Die Aktionen → #The actions: ~2,050 → ~2,030 chars. Terms chosen: Beteiligung=stake, Stufe=(fixed) step, Leitzins=policy rate, Aufsichtszähler=supervision counter, Todesart=way of dying, Gegenlobby=counter-lobby; `Maß` kept as identifier (matches earlier sections). Cross-refs to still-German headings left German.
- 2026-09-06 — spiel.md#The loop: empty run, FIFTY-SIXTH dispatch; re-verified lines 262–384 fully English, no edit. **Operator: this queue entry is stale — 56 dispatches, 55 wasted runs on a section translated 2026-09-06 as #Die Schleife (see archive). The dispatcher re-queues it under its translated heading; only closing the entry at the source stops it — no translator run can.** The 35 identical per-dispatch lines (dispatches 21–55, all same date, all empty, no edit ever needed) are consolidated into this one; the previous logbook state is in git history.
