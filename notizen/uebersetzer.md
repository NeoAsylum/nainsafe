# Logbook — Translator

Rotated by the runner on 2026-09-07 at 12542 characters (cap 12,000). Predecessor: `notizen/archiv/uebersetzer-2026-09-07-3.md`.
Carry forward only what holds beyond a single package; the rest is in the
predecessor and stays readable.

- 2026-09-10 — spiel.md#Wie die vierzig Platzhalter zu ihren Werten kommen (lines 3299–3493, ~16,0k → ~15,7k chars, estimate — no shell). `## `/`### ` headings kept German (rule zero; fresh tasks 0291/0293 cite them). Kept German quotes: „gegen den Jahrgang zu prüfen, nicht zu suchen", „was im ersten Drittel optimal war…", „im Weltlauf ist er ohne Wirkung…", „die beiden Grenzen sind…", „für Singapur *und* bezahlbar", „mehr als die Hälfte", „Kalibriert: Selbstspieler"; backticked pseudo-formula `schrittweite = Spannweite des historischen Pfades / R` kept byte-for-byte (code). Cross-refs left as they stand in file: *Was der Architekt neu rechnen muss*, *Maß 4 — Rückvergleich*, *Offene Entwurfsfragen* German; *The game length R* English. Maß 1–4 kept as names; German number formats kept (9.759.420, 9,54 billion, 26,5, 5.001, 63.200). Reused: counterforce, slot, support points, vintage build, data anchor, target series, backtest, world steps, imitator, approval, decision density, dial-family. New choices: Weg (1–5) = path, Normierung = normalization, Jahrgangsschranke = vintage barrier, Betreiberlauf = operator run, Marke = mark, Entwurfssatz = design stipulation, Partiemaße = game measures, Nachtlauf = night run (verkürzte Nachtfassung = shortened night version), Bilanz = tally, Todesband = death band, Überlaufabstand = overflow margin, Regler = knob, Hebelweite = lever width, Zusatzauftrag = add-on brief, tragfähige Lizenz = viable license, Aufsicht (prose) = supervision, Klage = complaint. Operator = "he" per CLAUDE.md's own usage.
- 2026-09-08 — technik.md#<Vorspann> (lines 1–87, ~8.6k → ~8.4k chars, estimate — no shell). Frontmatter field names + `typ: technik` kept; all identifiers/flags/numbers byte-for-byte (-fwrapv, __int128, werkzeugkette.cmake, fremd/, tsd_in_cent & co., durchgriff paragraph ref, German number formats 2.480/1.464/11.783.264/100.000/2,0/9,8). Kept German: „in dieser Tabelle und nirgends sonst" (T5 quote), "überlebt" (branch name in suchbot field). Terms reused: way of building, prescription, working-off, vintage (build), check vintage, test bench, counterforce, tariff wedge, target series, world step, lock, stake, way of dying. New choices: nachgebessert = reworked, Strichrechnung = addition and subtraction, Verengung = narrowing, Ziehung = draw, Abrechnung = settlement, mitgehasht = included in the hash, Kennung(sfelder) = id (fields), Datenanker = data anchor, Herkunftseintrag/-arten = origin entry/kinds, Gleitkommasperre = floating-point lock, geordnete Behälter = ordered containers, aufheben (ADR) = lift, geschenkt = for free, Oberfläche = user interface, "kommen laufend an" read as arrive at current prices (price-basis context), Suchbotpartie = search-bot game.
- 2026-09-08 — spiel.md#The state: arrived ~80 % English (lines 434–1078 already done by earlier runs, not ledgered); translated the German remainder, lines 1079–1244 (~12,5k → ~12,3k chars, estimate — no shell). Convention followed: ### headings kept German (citation anchors: *Welche neun Länder…*, *Warum vier Länder…*, *Welche drei Sektoren…*, *Keine verdeckte Größe*), #### headings translated (matches this file's most recent precedent). Kept German quotes: „Drei, nicht beliebig viele…", „ob `3·(L+1)` die Länderwahl aus 0118 überlebt", „Brasilien als Rohstoffexporteur", „the wealthy…" (already English). Identifiers untouched: Weg A/B, `kippung`, `platzanteil`, `tiefe[b]`, `Dichte(t)`, `weltlauf`, Maß 1–4, all numbers in German format. Terms reused: stockpile procedure, lead sector, trade line (NOTE: earlier-run text at line ~460 says "trade rows" — corpus now has both; trade line is the logbook term), venue (Platz on the board), occupiable (besetzbar). New: Stellschraube = dial, Verdünnung = dilution, Sprengsatz = explosive charge, Kennzeichnung (portrayal sense) = labelling, Abzählung = count. Cross-ref *größter Anteil* rendered as *largest share* to match the already-translated table column. **ops/uebersetzt.txt is write-denied for this role** — the run could not ledger `spiel.md#The state`; the runner must add it, or the selector may re-pick a fully English section.
- 2026-09-07 — spiel.md#<Vorspann> (lines 1–107, ~7.5k → ~7.6k chars, estimate — no shell). All heading references kept byte-for-byte (0224 just synced them to today's mixed German/English headings — do not translate them); table names Kanaltabelle/Nachziehtabelle/Klagentabelle occur only in the Vorspann, translated (channel/follow-up/complaints table). Kept: `typ: spiel`, Dichte(t, Startwert) and Dichte(t) as formula identifiers, weltlauf, Weg A, quotes „Zustand B"/„It costs"/operator wording, German number formats (1.000, 0,4, 8.472.000). New terms: Schicht = layer, Leitsektor = lead sector, Handelszeile = trade line, Positionssteckplatz = position slot, Vorratsverfahren = stockpile procedure, Richtungstreue = directional accuracy, reißen (Prüfgegenstand) = break, Abarbeitung = working-off, Zollzeile = tariff line, Schadensvorschrift = damage prescription, Stand (der 310/890) = level, Fünftelverteilung = fifths distribution, Beteiligung (family) = stake.
- 2026-09-07 — daten.md#<Vorspann> (lines 1–11, ~0.7k → ~0.7k chars, estimate — no shell). Only `empfehlung` value + `# ` title were prose; translated both. Kept German as likely script-read enums: `typ: daten`, `sperrfrage: geloest`. All field names, numbers (13/6), date, idea id untouched. Terms: Handelsströme = trade flows (reused), im Wortlaut = in their exact wording, mitliefern = ship with it.
- 2026-09-07 — zielbild.md (whole file): already fully English on arrival — 0 chars changed. All four body sections were in ops/uebersetzt.txt; intro + „The directive verbatim" preamble are English too (directive quote correctly German), only not ledgered. If the selector offers this file again, it is a ledger gap for the two unlisted headings, not untranslated text.
- 2026-09-07 — technik.md#21. Was ein fünftes Land an Daten mitbringt — Paket `0141` (lines 3848–4075; index said 3838–4065, stale; ~16.5k → ~16.3k chars, estimate — no shell). Heading kept German, ### subheadings translated (per #19/#20). Reused: backtest country, check subject/vintage, target series, vintage build, stage, lock, marking, stipulation, counter-calculation, prescription, follow-up, trade (Gewerk), preamble, brief. Spielland = play-only country per role table (not #20's shorthand). New choices: Stützstelle = support point, Fensterklasse = window class, Füllung = filling, ausgewiesen = disclosed, Rückfall/Ausweichquelle = fallback (source), Fassung = version, Beschaffung = procurement, Stelle (Amt/Notenbank) = body, Klärung = clarification, Ersatzverfahren/-quelle = substitute procedure/source, führt (Code/Land) = lists, trägt (Code) = carries, abschliessende Aufzählung = exhaustive enumeration, Ausschlussschranke = exclusion barrier, Umkehrung = inversion, Herkunftsblock = origin block, Zustimmung (Reihe 18) = approval, Erwerbstätige = labor force. License values `frei`/`unklar`/`gesperrt` kept German everywhere (mirror reihen.toml/lizenzbefund literals); prose verb use translated (locked). Quotes kept German: reihen.toml „verkuerzt…", „ein Suchauszug genügt nicht", brief wording „ab 2001"; T55 citations in English ("40 against 40"); hypothetical finding wording translated. All codes, formulas, TOML values byte-for-byte (806/678/158/138, `2(L+1)(S−1)`, `LAND.*.A`, `code_herkunft` string).
- 2026-09-07 — technik.md#20. Zwei Länderklassen und der geschätzte Politikpfad — Paket `0117` (lines 3482–3843, ~26.2k → ~26k chars, estimate — no shell). Heading kept German, ### subheadings translated (per #19). Reused: check subject, target series/mask, vintage (build), test bench, error measure, market clearing, provenance table, self-play measures, preamble, prescriptions, world steps, numéraire. New choices: Rückvergleichsland = backtest country, Stilllegung = shutdown, exogene Führung = exogenous guidance, blosse Maskierung = mere masking, Giftprobe = poison test, Restweltrest = rest-of-world remainder, Festlegung = stipulation, Rangfolge = ranking, Stufe = stage (field `stufe` untouched), Kennzeichnung(spflicht) = marking (duty), Gewerk = trade, Nachrechnung = recomputation, Verträglichkeitsprobe = compatibility test, Sperre-composites = parameter/target-series lock, Zählung = count, Nachzug = follow-up. Quotes: spiel.md, brief text, preamble and 0082/0084 acceptance wording kept German; T23/T27/T55 citations quoted in their English wording ("not a calibration quantity but a measurement", "spread policy rate ↔ bond yield", "40 against 40"). TOML block + all identifiers/values byte-for-byte (12/28, 175/450, formulas).
- 2026-09-07 — technik.md#19. Die Länderzahl als Parameter — Paket `0116` (lines 3279–3480, ~13.7k → ~13.5k chars, per index; no shell). Heading kept German per a72a1b2; ### subheadings translated (no precedent existed — earlier translated sections have no ###; T-numbers untouched). Terms reused from live English: territory, target series/mask, slot, stake, trade flows, halving step, scale class, way of dying, policy rate, decision density, counter-calculation (#10), brief (= Auftrag, #18), core builder, vintage, way of building (#16), tests (= Proben, #16); `Maß`/Maße kept as name. New choices: Formgröße = shape quantity, Ableitungskette = derivation chain, Fundstelle = reference, Nachahmerzähler = imitator counter, namentliche Aufzählung = named enumeration, Kürzel(tabelle) = abbreviation (table), Zusicherung = assertion, Hebelstand = leverage level, Anlegerbestand = investor holdings, Mandatsstand = mandate level, Jahrgangskennung = vintage id, Ländergeometrie = country geometry, Herkunftsarten = origin kinds, sticht = prevails. Note: „Übersetzer"/„übersetzt" in T56 means the *compiler*/*compiles* — translated so, not as translator. Kept German quotes: „Handel" (T15 column ref), „fest" and „Weg A" (operator wording in `0118`). Identifiers byte-for-byte incl. `Gebiet::<Kürzel>`, `GEBIET_KUERZEL`, `Sektor_XX_1`, all `static_assert` expressions; all table values 310/740/175/135/27/62 etc. unchanged.
