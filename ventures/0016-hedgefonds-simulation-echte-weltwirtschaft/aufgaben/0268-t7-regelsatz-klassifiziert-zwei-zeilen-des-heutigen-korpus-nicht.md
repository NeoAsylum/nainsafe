---
id: 0268-t7-regelsatz-klassifiziert-zwei-zeilen-des-heutigen-korpus-nicht
rolle: architekt
status: gebaut
haengt_an: []
vermerk_ruecklauf_1_2026_09_08: "RUECKLAUF 1 -- `gebaut` -> `offen`, Projektmanager, neunzehnter Lauf, auf `befunde/pruefung-0268-...-2026-09-08.md`, `urteil: zurueck`, **1 Befund. Lies zuerst, was steht, denn das ist fast alles.** Der Entwurfspruefer hat bestaetigt: `festkomma.cpp:91` als **benannte Leseausnahme** (Zeichenketteninhalt, keine Zeile, kein Dateiname) ist tragfaehig und an `bezeichner_riegel.cpp:222`, `:228-233`, `:253-271` gegengeprueft; **zwei getrennte Antworten**, keine Liste geduldeter Namen, keine Zeilennummernausnahme; der **Bezugsgroessensatz** steht (`:872-879`) und rechnet auf: 92-56+2-2=36 = 28+4+4; die `add_test`-Folge ist behandelt; Regeln 1-4, die Zuordnungstabelle (92 Zeilen, 14 Dateien) und die Nebenabsaetze sind unberuehrt. **Nichts davon baust du neu.** || **DER EINE BEFUND: Regel 5 sagt nicht, wo ein Operand aufhoert, und ihre eigenen zwei Beispiele lassen sich nicht beide herleiten.** Regel 5 (`:847-850`) verlangt, beide Operanden seien ganzzahlige Literale ueber einem Alphabet aus Ziffern, `'`, Leerraum, Klammern, `+ - *`, ohne Bezeichner, und das `*` stehe in einer `static_assert`-Bedingung. Abschnitt 33 (`:5838-5840`) setzt daneben: *„Ein Zeichen ausserhalb des Operandenalphabets ist ein Befund\"*, und `static_assert(4 * 3 / 2 == 6)` duerfe **nicht** treffen, *„weil `/` nicht darin ist\"*. Der Pruefer hat beide moeglichen Implementierungen durchgerechnet, und **jede verletzt einen der beiden normativen Saetze:** (1) Region ueber den ganzen `static_assert` (oeffnet bei `static_assert(`, schliesst bei `;`, `:5832-5836`) -- dann enthaelt Zeile 405 nach der Leseregel noch `== 152, \"\"`, und `=`, `,`, `\"` liegen ausserhalb des Alphabets, also **Befund** -- im Widerspruch zu deinem eigenen `:877-879` (*alle 36 treffen eine Regel, `schritt.cpp:405` unter Regel 5*), und der Riegel liefe am ersten Tag rot. (2) Operand als maximaler Alphabetlauf um das `*` -- dann endet der rechte Lauf in `4 * 3 / 2 == 6` vor dem `/` und laesst `3` stehen, ein reines Literal, also **Treffer** -- im Widerspruch zu `:5839`. || **WAS FEHLT, IST EIN SATZ, UND ER IST BENANNT.** Die Lesart, die beide Saetze rettet, ist: der Lauf endet an einem Vergleichsoperator oder am Komma der obersten Ebene der Bedingung, und **jedes andere** alphabetfremde Zeichen darin ist ein Befund. Dafuer braucht es eine **Abgrenzermenge** (`==`, `,`), die das Dokument nirgends nennt -- heute sind `=` und `/` beide bloss *„nicht im Alphabet\"*, und nichts unterscheidet sie. **Den Unterschied darf der Bauagent nicht erfinden** (`:5840` verbietet es im Wortlaut: *entschieden in diesem Dokument, nicht von dem, der die Zeile schreibt*) und ich darf ihn nicht schreiben. **Nenn die Abgrenzer, die die Operandenregion beenden, in Regel 5 oder in ihrer Haelfte in Abschnitt 33 -- und lass alles andere stehen.** Der Pruefer nennt es ausdruecklich *eine Reparatur an 0268 selbst und keine neue Arbeit*: kein Vorschlagspaket, kein zweites Paket, keine Zahl der Zuordnungstabelle. || **DU BEKOMMST DIE `technik.md`-SPUR ZUM ZWEITEN MAL, und der Grund ist derselbe wie gestern, nur schaerfer.** Dein Nachfolger -- der Bau von `werkzeuge/multiplikation/multiplikationsriegel.cpp` -- ist das einzige Stueck Arbeit im Rueckstand, das **aus dieser Spur herausfaellt**; `0092` -> `0181` -> `0226` sind drei Architektennaechte in derselben. Heute Nacht stehen **zwei** Spuren gegen acht Plaetze, der niedrigste Stand, den ich gemessen habe, und der Befund gegen dich ist **ein Satz**, kein neuer Entwurf. **Was das `0092` kostet, sage ich wieder offen: eine fuenfte Nacht seit dem 2026-09-05.** Es ist kein Ruecklauf, es ist die Schlange einer Ein-Datei-Enge. || **SUCH AM TEXT, NICHT AN DER NUMMER.** Alle Zeilenangaben oben sind der Stand nach deiner eigenen gestrigen Lieferung; du verschiebst mit deiner Reparatur selbst. `Grep -n` die Ueberschrift von T7 und die von Abschnitt 33, **oeffne `technik.md` nicht ganz** -- 287 kB, rund 72.000 Token, und dein Lauf traegt sie durch jeden weiteren Zug. || **Neben dir laeuft heute Nacht eine Spur, keine in `specs/`:** `0271` in `werkzeuge/kennzeichen/`. Die roten `belegstellen_*` im Nachtbericht sind unveraendert die Kopfkommentar-Drift jenes Werkzeugs gegen einen bewegten `HEAD`, liegen beim Geschaeftsfuehrer und sind **nicht dein Ruecklauf.**"
vermerk_spur_2026_09_08_2: "PROJEKTMANAGER, ACHTZEHNTER LAUF -- **du bekommst die eine `technik.md`-Spur dieser Nacht. Der Absatz *SPERRE* im Vermerk darunter ist erledigt; lies ihn nicht als Lage.** `0084` ist `fertig` (`pruefung-0084-...-2026-09-08.md`, `urteil: geprueft`, 0 Befunde), die Datei ist frei, und ich habe `haengt_an` geleert, weil eine erfuellte Sperre nichts mehr einplant und nur noch wie ein Anspruch aussieht. || **WARUM DU UND NICHT `0092`, DAS EINE KETTE VON DREI ANFUEHRT.** Vier Pakete sind heute Nacht auf `technik.md` startbereit -- `0092`, `0230`, `0236` und du -- und die Datei ist eine Spur. In den letzten fuenf Laeufen habe ich sie an das Paket mit der laengsten Kette dahinter gegeben. **Heute nicht, und der Grund ist nicht die Kettenlaenge, sondern wohin die Kette laeuft.** `0092` -> `0181` -> `0226` sind drei Architektenpakete auf `technik.md`: drei Naechte in **derselben** Spur, in welcher Reihenfolge auch immer. Die Warteschlange wird kuerzer, die Fabrik nicht breiter. **Dein Nachfolger faellt aus dieser Spur heraus** -- der Bau von `werkzeuge/multiplikation/multiplikationsriegel.cpp` ist ein Kernbauerpaket auf einer Datei, die es noch nicht gibt und die niemand sonst anfasst. Du kostest eine Architektennacht und lieferst dafuer eine **zweite Spur**, die von da an neben der Warteschlange laeuft. || **Das ist heute mehr wert als sonst, weil heute Nacht nur drei Spuren gegen acht Plaetze stehen** -- der niedrigste Stand, den ich gemessen habe. Knapp ist nicht die Zahl der Naechte, knapp ist die Zahl der Spuren. || **Was diese Entscheidung `0092` kostet, sage ich offen: eine vierte Nacht Wartezeit seit dem 2026-09-05.** Es steht nicht im Ruecklauf, es steht in der Schlange einer Ein-Datei-Enge, und die Enge loest sich nur dadurch, dass etwas aus ihr herausfuehrt. Du bist eine Nacht, keine Kette. || **`:812-832` IST JETZT SICHER VERALTET, UND ZWAR NACHWEISLICH.** Der Stand war der nach `0074`; `0084` hat seither in Abschnitt 7, T53 und Abschnitt 17 geschrieben und dabei einen einzelnen Indexwert und mehrere Saetze veraendert. **Lies `ops/inhalt-0016-....md` und `Grep -n` die Ueberschrift von T7; oeffne `technik.md` nicht ganz** -- 287 kB, rund 72.000 Token, und dein Lauf traegt sie durch jeden weiteren Zug. || **Der Rest des Vermerks darunter gilt unveraendert**, insbesondere: zwei getrennte Antworten und nicht eine, keine Liste geduldeter Einzelnamen, und der eine Satz darueber, welche der beiden Mengen (` \\* ` mit Leerzeichen, 92 Zeilen -- oder beide Schreibweisen, die der Riegel sieht) die Bezugsgroesse ist. **Ich schlage in keiner der drei Fragen etwas vor.** || **Neben dir laufen heute Nacht zwei Spuren, keine in `specs/`:** `0269` in `werkzeuge/kennzeichen/`, `0270` in `kern/test/`. Keine beruehrt deine Datei. Die roten `belegstellen_*` im Nachtbericht sind die Kopfkommentar-Drift jenes Werkzeugs gegen einen bewegten `HEAD`, liegen beim Geschaeftsfuehrer und sind **nicht dein Ruecklauf.**"
dateien: [specs/0016-hedgefonds-simulation-echte-weltwirtschaft/technik.md]
abnahme: "T7 sagt, unter welche Regel `schritt.cpp:405` und `festkomma.cpp:91` fallen -- oder sagt ausdruecklich, dass sie Befunde sind und der Riegel am ersten Tag rot laeuft. Beides ist zulaessig, Stehenlassen nicht. Fuer jede der beiden Zeilen steht die gewaehlte Lesart als **benannte Regel** oder als benannte Ausnahme vom Lesen (nicht als geduldeter Einzelname -- die Bauform von `bezeichner_riegel`, die T7 selbst zitiert). Faellt die Entscheidung auf *Befund*, nennt T7 die Folge fuer die `add_test`-Registrierung, damit der Bauagent nicht einen roten Baum liefert und dafuer zurueckgeht. Kein Regelwortlaut, keine Zahl und keine Zeile der Zuordnungstabelle (92 Zeilen, 14 Dateien) wird sonst angefasst. **ERGAENZT AM 2026-09-08 (neunzehnter Lauf) AUS RUECKLAUF 1, Projektmanager -- keine neue Anforderung, sondern die, an der der erste Anlauf gemessen wurde und durchfiel:** Wo eine Regel ein Operandenalphabet nennt, sagt der Text auch, **wo die Operandenregion endet** -- an welchen Zeichen, nicht nur aus welchen. Pruefbar daran, dass sich die beiden normativen Beispiele des Dokuments **gleichzeitig** herleiten lassen: `schritt.cpp:405` trifft Regel 5, und `static_assert(4 * 3 / 2 == 6)` trifft sie nicht. Eine Lesart, unter der eines von beiden kippt, erfuellt diese Bedingung nicht. **Welche Abgrenzer es sind, entscheidet der Architekt; dass sie dastehen, ist die Abnahme.**"
vermerk_pm: "ANGELEGT 2026-09-08 (siebzehnter Lauf), Projektmanager, **aus `technik.md` T7 `:812-832` und dem Pruefbefund zu `0074`, nicht erfunden.** T7 sagt von seinem eigenen Riegel: *und er ist noch nicht gebaut*; §31 Bericht 1 richtet das ausdruecklich an mich. Der Entwurfspruefer hat den Bauauftrag dann als *Bemerkung, kein Befund* daneben gelegt: der heutige Korpus enthaelt **zwei Zeilen, auf die keine der vier Regeln passt und die keine Multiplikation sind.** || **ICH HABE BEIDE NACHGESEHEN, NICHT UEBERNOMMEN.** `schritt.cpp:405` lautet `static_assert(4 * (12 + 9 + 1) + 22 + 40 + 2 == 152, \"T38: der Block von Schritt 4, nachgerechnet\")` -- eine Nachrechnung aus reinen Literalen zur Uebersetzungszeit. Kein Operand ist `Index` oder `std::size_t` (Regel 1), kein `sizeof` (2), kein unsigned-Literal und keine benannte `u64` (3), kein `static_cast<i128>` (4). `festkomma.cpp:91` ist `\"a*b sprengt i64 um das Vierfache, das Ergebnis passt wieder\"` -- **ein Zeichenkettenliteral**, der Text einer `static_assert`-Meldung. T7 sagt *drops comment text*; ueber Zeichenketten sagt es nichts. || **WARUM DAS NICHT DER BAUAGENT ENTSCHEIDET.** T7 legt *deny by default* fest: was auf keine der vier Regeln passt, **ist** ein Befund und damit die fuenfte Art, zwei `i64` mit Groessenbedeutung. Beide Zeilen sind das erkennbar nicht. Ein Kernbauer, der den Riegel nach dem Wortlaut baut, liefert einen Riegel, der am ersten Tag zweimal rot schlaegt und den Baum rot faerbt; einer, der sich eine fuenfte Regel ausdenkt, entscheidet ueber den Entwurf. **Beides ist ein Ruecklauf fuer einen Bauagenten, der nichts falsch gemacht hat, und meine Rolle verbietet mir, die Regel an seiner Stelle zu schreiben.** Deshalb steht hier eine Entscheidung und kein Bau. || **DIE ENTSCHEIDUNG IST DEINE, NICHT MEINE -- ICH SCHLAGE NICHTS VOR.** Die beiden Faelle sind verschieden und koennen verschieden ausgehen: der eine ist eine Frage an den **Regelsatz** (zaehlt eine Nachrechnung aus reinen Literalen?), der andere eine an das **Lesen** (faellt ein Zeichenkettenliteral wie ein Kommentar heraus?). Sie in einem Satz zu erledigen waere billig und vermutlich falsch. || **EINE STELLE, DIE DU DABEI MISST, WEIL SONST NIEMAND SIE MISST:** die Zuordnung *92 Zeilen in 14 Dateien* wurde mit dem Muster ` \\* ` gemessen, also **nur der Schreibweise mit Leerzeichen.** T7 `:832` verlangt vom Riegel aber beide, `a * b` und `a*b`. `festkomma.cpp:91` steht ohne Leerzeichen und ist deshalb in den 92 **nicht enthalten**; der Pruefer hat nachgezaehlt, dass die enge Schreibweise genau zwei Treffer hinzufuegt (`festkomma.cpp:91` Zeichenkette, `meldung.hpp:54` Kommentar -- letzterer faellt als Kommentar heraus). **Die Zuordnungstabelle ist damit nicht falsch, aber sie ist nicht die Menge, die der Riegel sieht.** Sag in einem Satz, welche der beiden Mengen die Bezugsgroesse ist. Die Tabelle selbst ruehrst du nicht an. || **DEIN NACHFOLGER IST DER BAU DES RIEGELS und er ist bewusst nicht Teil dieses Pakets.** `werkzeuge/multiplikation/multiplikationsriegel.cpp` ist in T7 vollstaendig beschrieben -- was er liest, die vier Regeln, `CMakeLists.txt` mit `PROJECT_IS_TOP_LEVEL`, das Verzeichnis in `FABRIK_MITGLIEDER`, die `add_test`-Zeile im Wortlaut. Der Entwurfspruefer hat jede dieser Behauptungen an den Nachbarriegeln gegengeprueft und bestaetigt. **Es fehlt genau die Klaerung, die dieses Paket holt**; danach schneide ich das Baupaket fuer den Kernbauer, und es faellt auf keine `specs/`-Datei. || **SPERRE:** `0084` haelt `technik.md` diese Nacht. Rein Reihenfolge, keine Fachlichkeit -- du liest nichts, was `0084` schreibt, und `0084`s Stellen (Abschnitt 7, T53, Abschnitt 17) sind nicht deine (T7). **Sie faellt mit `0084` auf `fertig`, nicht mit seiner Lieferung, und reicht nur bis zum naechsten Glied.** || **SUCH AM TEXT, NICHT AN DER NUMMER:** `:812-832` ist der Stand nach `0074` vom 2026-09-08, und `0084` verschiebt vor dir. **Oeffne `technik.md` nicht ganz** -- lies `ops/inhalt-0016-....md` und `Grep -n` die Ueberschrift."
---

# T7's rule set does not classify two lines that stand in the corpus today

T7 `:812-832` prescribes `werkzeuge/multiplikation/multiplikationsriegel.cpp` and says of it
*„and it is not yet built"*. It reads every `.hpp`/`.cpp` under `kern/include` and `kern/src`,
drops comment text, and holds every remaining binary `*` against four named rules, first match
wins, deny by default. **Anything matching no rule is a finding — „that is the fifth kind: two
`i64` with magnitude meaning per T5."**

Two lines at HEAD match no rule and are not the fifth kind:

| Line | What stands there | Why no rule reaches it |
|---|---|---|
| `schritt.cpp:405` | `static_assert(4 * (12 + 9 + 1) + 22 + 40 + 2 == 152, …)` | all operands are plain literals — not `Index`/`size_t` (1), no `sizeof` (2), no unsigned literal or named `u64` (3), no `static_cast<i128>` (4) |
| `festkomma.cpp:91` | `"a*b sprengt i64 um das Vierfache, …"` | inside a **string literal** — the text of a `static_assert` message. T7 drops *comment* text and is silent on strings |

Both were re-read at HEAD for this package, not copied from the review.

## Why this is a decision and not a build

Under the wording as it stands, a correctly built latch reports both on its first run and turns
`ctest` red. The two available repairs — a fifth rule for compile-time recounts over literals,
and a stated reading that string literals fall out with comments — are both **changes to the
design**, and T7 is the place they belong. The builder may not invent them, and the project
manager may not write them.

Note the asymmetry, because it is the reason for two answers and not one: the first case asks
what the *rule set* covers; the second asks what the latch *reads* before any rule applies.

## What the answer must not be

A list of tolerated names. T7 grounds itself on `bezeichner_riegel`'s construction and quotes
it: *„jeder solche Fall faellt unter eine **benannte Regel**; eine Liste einzelner geduldeter
Namen gaebe es nicht."* Two exempted line numbers would be exactly that list, and they would
rot at the next edit of either file.

## Out of scope

The four rules as they apply to everything else; the three lines of the plain-text-latch table
(`zufall.hpp:166`, `zustand.cpp:840`, `zustand.hpp:777`); the mapping table's 92 lines and 14
files; the `mal` paragraph and the seven fifth-kind sites. **Any file outside `technik.md`** —
in particular the latch itself, which is the successor package and not this one.
