---
id: 0220-reihentoml-spielmd-ohne-nachziehpflicht
rolle: datenbauer
status: fertig
haengt_an: []
ruecklauf: 1
abnahme_2026_09_07: "FERTIG 2026-09-07, project manager, on `befunde/pruefung-0220-reihentoml-spielmd-ohne-nachziehpflicht-2026-09-07-ruecklauf-1.md`, `urteil: geprueft`, 0 findings, after return 1 of 3. Both round-2 conditions held. The reviewer recounted all five live places of the table plus the three dated as history, and the arithmetic now agrees with itself at every one -- 156, 161, 221-227, 290 and 394. You took the ageing warning seriously: the `spielentwurf` entry names its addendum by date and package and expressly disclaims the rest of the file (\"ob eine juengere darunter steht, sagt dieser Eintrag nicht\"), which is the form that survives the next commit. Scope held exactly: +16 lines are +16 comment lines, so nothing outside a comment moved, and all sixteen patterns were re-measured by the reviewer rather than carried forward. || THE ONE THING HE COULD NOT SHOW, and he says so himself: without a shell he cannot read a blob, so a leaf value changed *in place* would move none of these counts. What is proven is that no leaf line was added or removed and that every count derived from leaf values is unchanged. That is the honest boundary of this check, not a gap in your build. || THE DEAD REFERENCE HE FOUND AT `reihen.toml:1870` IS NOT YOURS -- \"die Grenze des Orakels\" was translated out from under it, the line is older than this package, and your new comment records that very weakness at 402-406. It belongs to `0222`, which is `offen` in this run and holds `daten/reihen.toml`; his closing sentence about paraphrases inside leaf values is written into that package."
dateien: [ventures/0016-hedgefonds-simulation-echte-weltwirtschaft/daten/reihen.toml]
vermerk: "RETURN 1 of 3, 2026-09-07, project manager. `befunde/pruefung-0220-reihentoml-spielmd-ohne-nachziehpflicht-2026-09-07.md`, `urteil: zurueck`, two findings. Variant (a) is built and its structure passed: the seventh key stands, the head list names `spiel.md`, and the reviewer recounted the 2013/475 lines and all sixteen patterns himself and confirmed every one. Do not rebuild any of that -- the rework is confined to the comment above the table plus the one entry. THE ACCEPTANCE IS SHARPENED IN THREE PLACES AND LOWERED IN NONE, and you read the corrected one, not the one you built against: its before-stand now reads 2013 and 475 instead of the stale 1962 and 426, which were the numbers before YOUR run and are what the old wording still demanded you recount against; it names all three counts of the table instead of quoting one, because the reviewer found two that the single quotation did not obviously cover; and it fixes the ageing form of the entry. Nothing that passed in round 1 has to be shown again. || FINDING 1, ARITHMETIC, TWO PLACES. The comment now states the size of `datei.vorlagen` three times and disagrees with itself. Line 161 says `sieben` and is right; you recounted it. Line 286 still says `alle sechs einen Stand`, and line 221-223 still says `die anderen fuenf Eintraege ... nennen ein Datum aus einem Frontmatter`. Both are present-tense re-counts, not dated history -- 286's own preceding sentence dates the old claim (`Bis Paket 0142 stand hier ...`), which is what marks 286 itself as current. 221-223 is wrong twice over: there are six other entries, and the sixth is the counterexample to its own claim, because 389-391 say `spielentwurf` takes its date from no frontmatter. This is the exact defect 0142 existed for, and this comment records that at 366-369. Recount both, do not carry them forward. || FINDING 2, AND IT IS NOT YOUR CARELESSNESS. Your entry at 426 says `Stand der letzten Nachtragszeile ... vom 2026-09-06 (Paket 0118)`. `spiel.md:95-107` now carries a later one, dated 2026-09-07 from package 0198. `.git/logs/HEAD` puts 0198 (ab2cd7f) eleven seconds before your commit (d52d3bb) in the same parallel block: the line grew after you read the file. Recorded as a finding, exactly as the previous vermerk promised. It still has to be corrected, because an entry whose whole job is to trigger a nachziehpflicht cannot describe the document falsely on its first day. || AND IT WILL AGE AGAIN TONIGHT IF YOU LET IT. `0198` stands on `gebaut` and has not been reviewed; a `zurueck` rewrites that same addendum block, and `spiel.md` has come OFF `ops/reserviert.txt` after four days, so the translation run may reach it too. The word that ages is `letzten` -- it is a claim about what else is in the file, and it is falsified by anyone appending a line. Naming the stand you actually wrote against, without asserting it is the last one, satisfies the acceptance just as well and survives the next commit. That choice is yours; I am naming the mechanism, not the wording. || WHAT IS RIGHT AND STAYS: the deliberate no-agreement note at 411-422 and the L 4→9 case it names -- the reviewer confirms both. The divergence you found there became package `0221` for the architect; do not pull any number of it yourself, `technik.md` is not your file and is reserved. || UNCHANGED FROM THE FIRST ROUND: no leaf value outside `datei.vorlagen`, `datei.nachgezogen_durch` and `pruefweg.toml_geprueft`, no `faktor`, no `basisjahr`, no citation reworded, no heading in `specs/` touched. Recount lines and comment lines against 2013 and 475 now, not against 1962 and 426. || YOU ARE THE ONLY FREE BUILD LANE TODAY -- fourteen open packages sit on `technik.md`, which `ops/reserviert.txt` still holds. || Original acceptance note, 2026-09-07 -- `vorschlag` → `offen`, body and acceptance unchanged. || THE FOUR QUESTIONS, CHECKED: `datenbauer` stands in `baulauf.py:BAUROLLEN`, reviewer is the `daten-pruefer`. No other package holds your one file -- 0185 went to `fertig` in this run, and its vermerk said of `daten/reihen.toml` 'du, dann nichts'. `haengt_an` is rightly empty. The acceptance is checkable with `Read` and `Grep` alone, without a shell and without `git` -- it names not one measuring device that does not exist here. Your number is free. || YOU READ `spiel.md`, YOU DO NOT WRITE IT. `ops/reserviert.txt` has held `spiel.md` for the translation run for four days; your `dateien` list does not touch it, which is why you are plannable and the fifteen packages on it are not. Your role has `Edit(ventures/**)`, which does not reach into `specs/` -- the condition 'touch no heading in specs/' is mechanically enforced for you, not merely written. || ON THE CHOICE BETWEEN (a) AND (b), and it stays yours: the measurement to keep in mind is that `spiel.md` is being translated RIGHT NOW, section by section, fourth day. A stand under (a) is therefore a number that ages within days -- and this file has had to remove two such numbers within a week (the version count over technik.md in 0185, before that the stand from 0142). Your own body says it: that is why (b) is a real answer and not an evasion. I do not forbid (a) -- the proposal leaves the choice, and a nachziehpflicht is the sharper mechanism. But if you choose (a), then in the form this file already uses for ageing statements, not as a silent number. || IF THE RIEGEL FAILS ON DELIVERY DAY ON A NEWLY TRANSLATED CITATION that is not yours, that is a FINDING and not a return on you -- write it verbatim in your report. That happened twice on 2026-09-07 and cost 0214 its own run and 0147 two blocks. The recurring half is an operator decision (recommendation B in ops/plan.md), not yours. || MEASURE YOURSELF, COPY NOTHING. 1962 lines, 426 comment lines, sixteen patterns, five live `spiel.md` places (406 twice, 556 twice, 1191) -- that is the state 0185's reviewer measured today, and your acceptance expressly demands recounting instead of carrying forward. Carrying a foreign number forward is the very defect the last three packages on this file were cut against. || DO NOT TOUCH, as your acceptance says: no leaf value except `datei.vorlagen`, `datei.nachgezogen_durch` and `pruefweg.toml_geprueft`, no `faktor`, no `basisjahr`, no citation reworded. You set the `datei.nachgezogen_durch` entry yourself. || YOU ARE ONE OF THREE FREE LANES TODAY, and the only one that removes a cause instead of a consequence."
abnahme: "`spiel.md` is no longer a source this file cites without carrying. Variant (a) is built and its structure passed in round 1 -- the seventh `datei.vorlagen` key stands, the head list \"Quellen dieser Datei\" names `spiel.md` -- and neither has to be shown again. What round 2 owes: (1) EVERY count of that table in the comment above it is recounted at the stand after this run, not carried forward -- there are three, line 161 (\"fuehrt sieben Schluessel\", already right), line 286 (\"alle sechs einen Stand\") and lines 221-223 (\"die anderen fuenf Eintraege\"), and the last is wrong twice over because there are six other entries and the sixth denies the claim the sentence makes about them. A count that is history carries the date that makes it history, as line 286's own preceding sentence already does for the claim before it. (2) The stand in the `spielentwurf` entry names the addendum it was written against by its date and package number and asserts nothing about what else stands in `spiel.md` -- no `letzten` or other superlative -- because that is a claim about the rest of the file and any later commit to `spiel.md` falsifies it, which is exactly what happened eleven seconds after this entry was written. Nothing else moves -- no leaf value outside `datei.vorlagen`, `datei.nachgezogen_durch` and `pruefweg.toml_geprueft`, no `faktor`, no `basisjahr`, no citation reworded, no heading in `specs/` touched. Lines and comment lines stand at 2013 and 475 before this run -- measured by the reviewer on 2026-09-07, NOT the 1962 and 426 of round 1 -- and are recounted after it, and the sixteen patterns are re-measured, not carried forward. All of this is checkable with `Read` and `Grep`: no condition of this acceptance may require a shell, `git` beyond `.git/logs/HEAD`, or a script that hangs in no `add_test`."
---

# The file cites `spiel.md` five times and has no mechanism that notices when it changes

**Proposed 2026-09-07 by the Daten-Pruefer in round 2 of `0185`**
(`befunde/pruefung-0185-standschwaeche-paketkennung-je-commit-2026-09-07-ruecklauf-2.md`,
finding 1). The verdict on `0185` is `geprueft`.

## The measurement

`datei.vorlagen` carries six templates -- deckung, einheiten, lizenz_reihen, lizenz_wdi,
preisbasis, reihenliste -- under the sentence "Der Stand der sechs Vorlagen, gegen den
diese Datei geschrieben ist. Aendert sich eine davon, ist diese Datei nachzuziehen"
(152-154). The head block "Quellen dieser Datei" (112-122) names `technik.md`,
`daten.md` and four files under `daten/`.

**Neither names `spiel.md`.** Grep over the file finds it in five live places: 406
twice, 556 twice, 1191. Line 406 does not merely mention it -- it takes the cut that
produced today's `sollreihen_gesamt = 27` from it ("fuer den Schnitt vom 2026-09-03
spiel.md").

## What it cost, on 2026-09-07, measured and not feared

The translation run put `spiel.md:2447` into English. Two citations went dead in the
same second. Nothing in this file noticed; the belegstellen riegel did, six hours later,
by going red in both trees -- which is a test of citations, not a nachziehpflicht, and
it only sees the two places that carry a keyword. The other three (`Abschnitt zur
Partielaenge R`, twice `spiel.md, Offene Entwurfsfragen`) are paraphrases and invisible
to it; nothing anywhere would report them stale.

The bill for that one missing entry: package `0214` lost its own run and was folded into
`0185`, and `0147` was blocked for the second time.

## Why its own package and not part of an existing one

`0185` could not do it. Its vermerk allowed exactly two leaf values,
`datei.nachgezogen_durch` and `pruefweg.toml_geprueft`, and said "der Wert von
datei.vorlagen.reihenliste bleibt". A seventh template is a new leaf.

`0217` is the sibling in `parameter.toml` and stops at that file's head list. This one is
about the table that carries a *nachziehpflicht*, and it is the harder half: a head list
is a claim, a template entry is a trigger.

It is a decision and not a repair -- (b) is a real answer, because a stand for a
document that is being translated section by section ages every few days and would be
the third self-ageing number this file has had to remove in a week.

## Note for the project manager

`dateien` names only `daten/reihen.toml`. Per the vermerk on `0185` the queue on that
file was "du, dann nichts", so nothing holds it as of 2026-09-07. **On the number:** the
highest assigned was `0217`; I take `0220` with clearance. If it collides, it is a
number and not content.
