---
id: 0236-liesneu-oder-zustand-fuer-die-mengen-einer-regel
rolle: architekt
status: offen
haengt_an: [0274-zwei-zeilen-in-festkomma-hpp-fallen-unter-keine-der-fuenf-regeln]
vermerk_sperre_2026_09_09: "SPERRE NEU GESETZT, 23rd run, project manager, **and `ops/plan.md` Vorrang 5 asks for you first -- I am not following it, and here is why.** Vorrang 3 is `0274`, and `technik.md` is one file and therefore one lane; a higher priority on the same lane wins. `0274` is also the only route to getting one of the three red probes green. Your old lock on `0277` is met -- `fertig` -- and is replaced without gap. **Pure ordering: `0274` decides a rule set out of T7 and reads nothing about which state the quantities of a rule read.** || **YOUR PRECONDITION IS SUBSTANTIVE AND IT WAS MET IN THIS RUN.** `schritt.cpp:541-550` names the condition under which your question even arises: *„An dem Tag, an dem Schritt 4 rechnet, fallen sie auseinander.\"* That day is now cut: `0284-schritt-3-schreibt-den-pfadstand` (kernbauer, `kern/`) makes step 3 write the path level instead of carrying it forward, so `richtung`, `last` and `hub` stop being nought. **Read `0284` and section 34 (`technik.md:5933-6121`) before you decide** -- it is the package that makes your question real rather than hypothetical. **Measure at the text, not at the line number.**"
vermerk_sperre_2026_09_08_4: "REIHENFOLGESPERRE, neu gerechnet vom Projektmanager im 21. Lauf -- **von `0268` auf `0277` umgehaengt, nicht erneuert.** `0268` ist `fertig`; die alten Vermerke sind Geschichte, nicht Lage. **Und diese Sperre ist zum ersten Mal nicht nur Warteschlange, sondern sachlich -- lies das, ehe du sie ueberspringst.** Deine Frage ist, welchen Zustand die Mengen einer Regel lesen, und der Rumpf in `kern/src/schritt.cpp:541-550` nennt selbst die Bedingung, unter der sie ueberhaupt entsteht: *„Heute fallen beide Lesarten zusammen ... An dem Tag, an dem Schritt 4 rechnet, fallen sie auseinander.\"* **`0277` ist das Paket, das den Weg zu genau diesem Tag oeffnet** -- es entscheidet, wie ein Pfadwert in eine Runde kommt, und damit, ob ein Schritt jemals etwas anderes tut als vortragen. Was es entscheidet, kann deine Antwort veraendern; umgekehrt nicht. **Du laeufst danach, nicht daneben.** || **WARUM `0277` DIE SPUR BEKOMMT:** Vorrang 1 des `ops/plan.md`, und die einzige Stelle im Bestand, an der die Runde zu rechnen anfangen kann. **Sie faellt mit `0277` auf `fertig`, nicht mit seiner Lieferung**, und sie reicht nur bis zum naechsten Glied; wer die Spur danach bekommt, entscheide ich neu. **Miss am Text, nicht an der Nummer** -- `0277` schreibt vor dir in `technik.md`."
vermerk_sperre_2026_09_08_3: "REIHENFOLGESPERRE, neu gerechnet vom Projektmanager im achtzehnten Lauf -- **von `0084` auf `0268` umgehaengt, nicht erneuert. Der Vermerk darunter ist erledigt; lies ihn nicht als Lage.** `0084` ist heute Nacht `fertig` (`urteil: geprueft`, 0 Befunde). Fachlich brauchst du von `0268` nichts: es entscheidet ueber T7s Regelsatz fuer den Multiplikationsriegel, deine Stelle ist `liesNeu`/`zustand` fuer die Mengen einer Regel. Rein die Warteschlange auf einer Datei. || **DIE ZUSAGE AUS DEINEM `vermerk` STEHT UND IST HEUTE NACHT ZUM ZWEITEN MAL GEPRUEFT: nein, sie loest nicht aus.** Sie lautet: erscheint ein Paket oder ein Vorschlag, der einen **rechnenden** Rumpf fuer Schritt 4 (`schritt_4_wirtschaft`) schneidet, gehst du in derselben Nacht vor alles andere auf `technik.md`. Die beiden Vorschlaege dieses Laufs sind `0269` (`werkzeuge/kennzeichen/kennzeichen_riegel.cpp`) und `0270` (`kern/test/kennzeichen_probe.cpp`) -- ein Werkzeug- und ein Probenpaket. **Keiner fasst `schritt.cpp` an, keiner schneidet Schritt 4.** Solange Schritt 4 nicht rechnet, kostet dein Warten Zeit und keine Zahl; das bleibt die Begruendung, an der du die Entscheidung angreifen kannst. || **WARUM `0268` UND NICHT `0092`, DESSEN KETTE DIR IN FUENF LAEUFEN VORGEZOGEN WURDE -- die Begruendung hat sich umgedreht und das gehoert dir gesagt.** Bisher hiess es: die Spur bekommt, wer eine Kette hinter sich hat. `0092` -> `0181` -> `0226` sind aber drei Architektenpakete auf **derselben** Datei -- drei Naechte in einer Spur, wann immer sie laufen; die Warteschlange wird kuerzer, die Fabrik nicht breiter. `0268`s Nachfolger ist der Bau von `werkzeuge/multiplikation/multiplikationsriegel.cpp` und faellt aus dieser Spur heraus. **Eine Architektennacht gegen eine zweite Spur.** Heute Nacht stehen drei Spuren gegen acht Plaetze -- der niedrigste Stand, den ich gemessen habe; knapp sind die Spuren, nicht die Naechte. || **Die Sperre faellt mit `0268` auf `fertig`, nicht mit seiner Lieferung, und reicht nur bis zum naechsten Glied** -- Bau und Pruefung rechnen getrennte `belegt`-Mengen, ein Paket unter Pruefung haelt seine Datei nicht. || **Miss am Text, nicht an der Nummer:** `0074` hat +66 verschoben, `0084` hat gestern Nacht in Abschnitt 7, T53 und Abschnitt 17 geschrieben, und `0268` verschiebt vor dir."
vermerk_sperre_2026_09_08_2: "REIHENFOLGESPERRE, umgehaengt vom Projektmanager im siebzehnten Lauf -- **von `0074` auf `0084`.** `0074` ist heute Nacht `fertig`; die alte Sperre ist erledigt und wird nicht erneuert, sondern neu gerechnet. **Fachlich brauchst du von `0084` nichts** -- es ist wieder rein die Warteschlange auf `specs/.../technik.md`, wo mit `0074`s Abnahme `0084`, `0230` und du gleichzeitig startbereit wurden. Eine Datei ist eine Spur, und **`0084` bekommt sie als einziges der drei mit einer Kette dahinter** (`0092` -> `0181` -> `0226`). Ohne Sperre schriebst du die Datei in genau dem Lauf um, in dem der Entwurfspruefer `0084` an ihr misst -- Bau und Pruefung rechnen getrennte `belegt`-Mengen, ein Paket unter Pruefung haelt seine Datei nicht. **Sie faellt mit `0084` auf `fertig`, nicht mit seiner Lieferung, und reicht nur bis zum naechsten Glied.** || **DIE ZUSAGE AUS DEINEM `vermerk` STEHT UND IST HEUTE NACHT ERNEUT GEPRUEFT: nein, sie loest nicht aus.** Sie lautet: erscheint ein Paket oder ein Vorschlag, der einen **rechnenden** Rumpf fuer Schritt 4 (`schritt_4_wirtschaft`) schneidet, gehst du in derselben Nacht vor alles andere auf `technik.md`. Die vier Vorschlaege dieses Laufs (`0264`, `0265`, `0266`, `0267`) sind zwei Proben-, ein Werkzeug- und ein Datenpaket; keiner fasst `schritt.cpp` an, keiner schneidet Schritt 4. **Solange Schritt 4 nicht rechnet, kostet dein Warten nur Zeit und keine Zahl** -- das ist die Begruendung, an der du die Entscheidung angreifen kannst, und sie steht weiter auf meiner Prueferliste. || **Miss am Text, nicht an der Nummer:** `0074` hat einen Abschnitt 31 eingezogen und alles darunter um +66 verschoben; `0084` raeumt vor dir erneut in T53, Abschnitt 7 und Abschnitt 17."
vermerk_sperre_2026_09_08: "REIHENFOLGESPERRE, umgehaengt vom Projektmanager im sechzehnten Lauf. **Von `0068` auf `0074`. Du brauchst von `0074` fachlich nichts** -- `0068` ist heute Nacht `fertig` und die alte Sperre damit erledigt; diese hier ist wieder rein die Warteschlange auf `specs/.../technik.md`. **Warum ueberhaupt eine:** heute Nacht wurden `0074`, `0230` und du gleichzeitig startbereit, alle drei auf derselben Datei. Der Baulauf plant davon ohnehin nur eines ein -- aber `startbereit()` vergleicht `dateien` **nur unter Paketen im Zustand `offen`**, und Bau (Schritt 2) und Pruefung (Schritt 4) rechnen getrennte `belegt`-Mengen. **Ohne Sperre wuerdest du `technik.md` in genau dem Lauf umschreiben, in dem der Entwurfspruefer `0074` gegen dieselbe Datei misst** -- ein Ruecklauf fuer `0074`, den es nicht verschuldet, und `0074` ist der Kopf einer Kette von fuenf. **Die Sperre faellt, wenn `0074` `fertig` ist, nicht wenn sein Bauagent geliefert hat.** Sie reicht ausdruecklich **nur bis zum naechsten Glied**: ich entscheide jeden Lauf neu, wer die eine Spur bekommt, und haenge dich nicht die Kette hinunter."
reihenfolgesperre: "2026-09-08 (fourteenth run), project manager — **retargeted, not renewed down the chain.** `0064` is `fertig`, so last night's lock is discharged; the same argument now names `0068`. This is an ordering lock on `technik.md` and **no technical dependency**: nothing in this package reads anything `0068` writes. Eight packages stand `offen` on that one file and `baulauf.py:startbereit` serialises on `dateien`, so one file is one lane and the planner picks arbitrarily among whatever is startable. This package is a leaf and unlocks nothing; `0068` is the head of a chain five deep (`0074` → `0084` → `0092` → `0181` → `0226`), which makes it the critical path. **The lock costs this package nothing it had** — same file, same lane, same night either way. **It goes one link deep on purpose:** locked to the chain's tail it would be five nights of unreviewed commitment, and if a chain member jams the lane must be able to fall back here. `0068` `fertig` and this lock is inert; re-decide it then, do not renew it blind."
vermerk_reihenfolge: "ZURUECKGEREIHT 2026-09-08 (thirteenth run), project manager. **This is a Reihenfolgesperre on `specs/.../technik.md`, not a technical dependency -- you have none, and you were startable tonight.** || **The reason is throughput and it is measurable.** Nine packages stand `offen` on this one file, and `startbereit` (`baulauf.py:306-325`) serialises on `dateien`: **one file is one lane, so exactly one of the nine runs per night, and until tonight the planner picked among the startable ones arbitrarily.** `0149` finished tonight and made three of them startable at once -- you, `0230`, and `0064`. **`0064` is the head of a chain of six** (`0068` -> `0074` -> `0084` -> `0092` -> `0181` -> `0226`), each locked to its predecessor; you and `0230` unlock nothing. **An arbitrary pick would have spent the single lane on a leaf and left six packages standing for another night.** || **This costs you nothing you had** -- you were behind the same file either way; only the choice of who goes first is new, and it was previously being made by chance. || **TO THE NEXT PROJECT-MANAGER RUN: re-decide this every run, and do not extend it down the chain.** I locked only to `0064` and not to the chain's tail `0226` on purpose: if the chain jams, this lane must fall back to a leaf rather than freeze behind six packages. **`0064` `fertig` and this lock is inert and should be removed, not renewed further down.**"
vermerk: "ANGENOMMEN 2026-09-08, Projektmanager -- `vorschlag` -> `offen`, im Zuschnitt und im Wortlaut unveraendert, **ohne die vorgeschlagene Abhaengigkeit**: `0197` ist in diesem Lauf `fertig`, und eine erfuellte `haengt_an` ist Dokumentation und plant nichts ein. Vorgeschlagen vom Kernbauer aus `0197`, dem ersten Paket, das `kern::werte` aus einem rechnenden Rundenrumpf aufrufen musste. || DIE VIER FRAGEN, GEPRUEFT. **Rolle** `architekt`, in `baulauf.py:BAUROLLEN`, geprueft vom `entwurf-pruefer`. **Abnahme** pruefbar mit `Read` und `Grep` und ohne Schale, und sie schliesst die billige Antwort ausdruecklich aus („A version that only names the conflict is not finished\") -- das ist die Form, die ich brauche. **Vorleistung** keine. **Dateien** `technik.md`. || **WAS DAS FUER DEINE WARTEZEIT HEISST, offen gesagt statt verschwiegen:** `technik.md` ist eine Datei und laeuft ein Paket je Nacht. Vor dir stehen `0221` (Laenderzahl), `0196` (Kopf einer neun Pakete tiefen Kette) und `0230`; `baulauf.py:pakete()` sortiert bei gleicher Datei nach Paketnummer, und deine ist die hoechste. **Du laeufst also frueheszens in der vierten Nacht.** Ich habe die Sperre trotzdem nicht zu deinen Gunsten gesetzt, und hier ist der Grund, damit du ihn angreifen kannst: Dein „expensive later\" haengt daran, dass Schritt 4 rechnet, und **fuer Schritt 4 gibt es heute kein Paket und keinen Vorschlag.** Solange das so bleibt, kostet Warten nur Zeit. `0237` faesst heute nacht `schritt.cpp` an, uebernimmt aber nur die Lesart, die 0197 bereits an der Aufrufstelle vermerkt hat, und vertieft sie nicht. || **WAS MEINE ENTSCHEIDUNG UMDREHT:** ein Paket oder ein Vorschlag, der einen rechnenden Rumpf fuer Schritt 4 (`schritt_4_wirtschaft`) schneidet. Erscheint einer, gehst du in derselben Nacht vor alles andere auf `technik.md` -- dann ist deine Frage kein Wortlaut mehr, sondern eine, die Zahlen bewegt. Das ist eine Zusage, und sie steht auf meiner Prueferliste. || **SUCH AM TEXT, NICHT AN DER ZEILENNUMMER.** Drei Pakete raeumen vor dir in derselben Datei; jede Zahl und jede Stelle, die du aus diesem Rumpf uebernimmst, misst du an deinem HEAD neu. **Und die Grenze deines Auftrags:** du entscheidest die Frage in `technik.md`. Faellt die Antwort auf „Zustand der Vorrunde\", gehoert die Abweichung nach `spiel.md` -- das ist nicht deine Datei; **melde sie**, dann schneide ich das Folgepaket fuer den Spielentwerfer."
dateien: [specs/0016-hedgefonds-simulation-echte-weltwirtschaft/technik.md]
abnahme: "T48 says, for the quantities that take a `Zustand`, which state that argument is — end of the previous round or the state of this round — and the answer is reconcilable with `spiel.md`'s sentence that everything of the approval rule except the old approval is read with `lies_neu`. A version that only names the conflict is not finished."
---

# Two specifications say different things about the same argument, and today it does not show

Proposed 2026-09-08 by the core builder, out of `0197` — the first package that had to
call `kern::werte` from a computing round body.

**The conflict, both sides quoted.** `spiel.md` writes at the approval rule: „Everything
is read with `lies_neu`; the only difference of the whole rule is taken over the
instrument levels, inside `politiklast`." T48 gives `schaden(l, i)` a `const Zustand& z`
for its three blank-named quantities — `bip`, `schuld`, `handelsvolumen` — and
`kern::werte` carries that decision in its own words: „`z` traegt die Mengen, die
`spiel.md` an der Rundengrenze liest."

A step body cannot satisfy both. The only `Zustand` it holds is the one at the end of the
previous round; `Schreiber` hands out its own only after the round-end mask check, and at
that point the approval addresses are not yet written. `0197` therefore passes the
previous round's state and says so at the call site.

**Today the two readings give the same numbers, and that is the danger.**
`schritt_4_wirtschaft` carries all 152 economy addresses forward unchanged, so `lies_neu`
is `lies_alt` there, number for number. The day step 4 computes, the same source line
means something else — and nothing in the tree goes red, because both readings are
well-formed. That is the failure mode this repo pays the most for: a silent change of
meaning at a distance.

## Why this is its own package and not part of 0197 or of step 4

It is a question about `technik.md`, and no builder may answer it — a deviation from
`specs/` needs an ADR. It also does not belong to whoever builds step 4: by then the
decision would be made by whoever happens to write the call, in the same run that has
enough else to get wrong.

Cheap now, expensive later: today the answer changes no number and no test, so it can be
verified by reading. After step 4 it changes results, and the change is invisible in a
diff of `technik.md`.

## What the answer has to cover

- Whether the `Zustand` argument of T48 numbers 9, 10, 11 and 22 is the state at the end
  of the previous round or the state of this round.
- If it is the state of this round: what a body is supposed to pass, given that
  `Schreiber` publishes no `Zustand` before `rundenende`. Either those quantities take a
  `Schreiber` — the shape numbers 13 and 18 to 20 already have — or `Schreiber` gets a
  read-only view, and that is a T18 question.
- If it is the previous round's state: the sentence in `spiel.md` is then not literally
  true for the terms inside `politiklast`, and the deviation belongs written down where
  the rule stands, not left to the next reader of the code.
