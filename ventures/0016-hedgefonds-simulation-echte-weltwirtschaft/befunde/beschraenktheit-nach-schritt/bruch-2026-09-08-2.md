# The stand for package 0243 — built, not run

Written 2026-09-08, after `bruch-2026-09-08.md` in this folder. **Nothing here is a
measurement.** No output file of this stand exists yet and none will exist during this
run — my role has no tool that executes a program (`agents/rollen/bruchtester.md:7-14`,
`agents/lauf.py:NIE`). Everything below is a prediction, written down **before** the run
so it can be wrong. The last measured numbers are still 0145's of 2026-09-06 in
`befunde/beschraenktheit/`.

**Not compiled either.** `-Werror` with `-Wconversion -Wold-style-cast` is unforgiving. A
translation error on the first run is possible; it is a defect of this stand, not a
finding about the model.

## What was built

`lauf.sh` and `beschraenktheit-nach-schritt.cpp` here. One command, no arguments. Own
build dirs `bau-sanitizer-{on,off}` under this folder (`.gitignore:246`), ctest listed to
file before and after and diffed, the program hand-compiled with `FABRIK_STRENGE` and the
overflow switches, nothing deleted, `ventures/.../bau` untouched.

**Three things made the 0145 program unusable, not one:** `schritt` took a fourth input in
0229 (the old 3-argument call no longer compiles); the round holds `parameter_pruefsumme`
against `partie.parameter_pruefsumme` since 0228, which the old start state leaves at `0`;
and the old start state sets no Zustimmung, so all four sit at `0`, inside the clamp
(Finding 2 of the earlier report). All three are fixed here. Zustimmung start values are
the probe's four — `{10'001, -1, I64_MAX, I64_MIN}` (`schritt_probe.cpp:1333-1334`).

## What each of the six files will contain

`lauf-sanitizer-<on|off>-<lage>.txt`. Both profiles are predicted **identical in content**;
the abort is a thrown `std::domain_error`, not UB, so the sanitizer sees nothing extra.

**`gueltig`** — return 0, 200 rounds. Start mask `0`; Zustimmung is matched by none of the
ten patterns, so the bound verdicts stay comparable to 0145 (ten × `GEHALTEN`). Round 1:
`geaendert ggue. Vorrunde 5` — `partie.runde` plus the four clamped Zustimmungen. Rounds
2–200: `ggue. Vorrunde 1`, `ggue. Start 5`. Closing: `5 von 310` after one round and after
200. The four-line block reads `4 von 4 auf der Schranke, 0 von 4 stehen geblieben`
(10'000 / 0 / 10'000 / 0). Chain 175, as in 0145. Checksums differ from 0145's — two more
addresses carry a value.

**`null`** — return 2, abort **in round 1**. `bip(US)` is `0`, so `realeinkommenshub`
(`schritt.cpp:629`) hits the denominator condition before any address is written. Wording
begins `kern::schritt -- Zustimmungsregel: bip(l) > 0 ist die Nennerbedingung`, names
`land.US.zustimmung`, ends in `0`. Start mask `535`, as in 0145. 0145 ran this profile 200
rounds green; that is the whole change.

**`decke`** — return 2, abort **in round 1**, inside `kern::werte::bip` at the **second**
address: `0 + 2^62` fits, `2^62 + 2^62` does not. Start mask `512` (Sektoranteile only,
first address `land.US.sektor.1.wertschoepfung`). Wording begins `kern::werte::bip -- die
Wertschoepfungssumme verlaesst i64 (T7)`, names `land.US.sektor.2.wertschoepfung` with
`4611686018427387904`, `die Summe davor war 4611686018427387904`.

## What falsifies what

| Print | What it refutes |
|---|---|
| `gueltig`: `0 von 4 auf der Schranke, 4 stehen geblieben` | 0197 — step 5 carries forward after all |
| `gueltig`: `ggue. Vorrunde` ≠ 1 from round 2 on | Finding 1 — something else moves, the clamp is not the whole motion |
| `gueltig`: any abort, or a mask ≠ 0 | a bound in `schritt` this stand did not account for |
| `gueltig`: chain ≠ 175 | the weltlauf mask changed since 0145 — someone else's finding |
| `null`: 200 rounds green | step 5 does not run on a fresh state; my Finding 3 is wrong |
| `decke`: `plus: Summe ausserhalb von i64 (T7)` | 0242's located ceiling message is not effective on this path |
| `decke`: no abort | 2^62 × 3 does not reach `bip` — step 5 is not the only reader |
| any: ctest list `vor` ≠ `nach` | this stand touched the tree; the whole run is void |

Both `ctest -N` lists are expected to show 26 entries in the workspace root
(`befunde/uebersetzung-2026-09-08.md`, 26 of 26 green at authoring time).

## Correction to `bruch-2026-09-08.md`, Finding 4

Its "**Should:** the same located message as the floor case" **has been built since** —
`kern/src/werte.cpp:723-751`, package 0242, commit `f2338d9`. The `decke` profile is
therefore a confirmation run for 0242, not a demonstration of a gap. The same report's
note that the tree is red is likewise overtaken: `verlauf_probe` passes.
